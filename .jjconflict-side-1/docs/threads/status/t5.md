# T5 status

- **Current item:** **CLOSED — VT lib-layer parity with ghostty is comprehensively
  validated; all VT features + correctness shipped; sole remaining divergence is the filed,
  budgeted #178.**
- **Last merged:** #223 (mouse-tracking oracle dim), 2026-07-14.
- **Blockers:** none.
- **Claims:** none (all dropped).

## Handoff

### (a) What shipped

- **VT features (umbrella #25):** OSC 8 hyperlinks (#26), DSR strictness (#31), DECRQSS
  (#27), OSC color queries (#28), mouse state flags + OSC 22 (#33), XTMODKEYS (#36),
  XTGETTCAP (#37/#157), XTWINOPS size reports/XTVERSION/enquiry (#32/#35). Earlier
  correctness: panic fix (#101), search cluster (#134/#142), reflow width cluster
  (#147/#150/#151/#153).
- **This session — a differential-testing harness + the bugs it caught:**
  - Harness (all in `crates/vt-diff`): a deterministic 20k-case **generative sweep**
    (`tests/generative_sweep.rs`, ~40 op families) + **AFL corpus replay** of ~3,981 ghostty
    seeds (`tests/afl_corpus.rs`, env-gated local-only) + a multi-dimension **oracle**
    comparing plain text, styled VT (SGR), cursor, and scalar state (pending-wrap /
    alt-screen / cursor-visible / mouse-tracking / scrollback-rows). PRs #161 #170 #199 #211
    #223.
  - Engine bugs it found + fixed: tabstop `unset` XOR-toggle parity (#161); `ModeState.saved`
    per-mode defaults for XTRESTORE (#170); OSC non-UTF-8 dispatch (#169 via #179); VPR/CUP
    `≥65536` overflow clamp (#179); ED must reset row `wrap` flag → reverse-wrap parity
    (#188). Plus an oracle-mapping fix (MOUSE_TRACKING reads mode-flags not the enum, #223).
  - Verify: `GHOSTTY_VT_LIB_DIR=~/local/ghostty/zig-out/lib cargo test -p vt-diff --features reference`
    (add `GHOSTTY_FUZZ_FULL=1` for full cmin). See [[generative-differential-sweep]] memory.
- **Emergency (cross-territory):** renderer build-break from #210's botched merge was fixed
  on trunk by `fb29aaf7` (landed independently); my duplicate hotfix #217 was closed.

### (b) What remains, and who owns it now

- **#178 — the one open engine divergence.** DECCOLM (mode 3/40) with a live OSC-133 prompt:
  ghostty's resize pushes the active rows into **scrollback** (decisively confirmed: ref
  scrollback 0→24) so `eraseDisplay(.complete)` preserves them; ours erases in place. Needs
  (1) a PageList `promptIterator`/`rowIterator` — **T1 territory** (`pagelist/iter.rs`) — and
  (2) the resize prompt-aware scrollback-push semantics. Fully characterized on the issue.
  Budgeted: `CMIN_KNOWN_DIVERGENCES = 5` in `afl_corpus.rs`. **Cross-territory → coordinate
  with T1.**
- **Not T5's:** `627518447` (ScreenSearch unported); `a55850c98` + `b287f6d1a` grapheme-assert
  in T1's `print.rs` (already Inboxed to T1).
- **Optional harness strengthening (low bug-yield):** remaining oracle dims — kitty-keyboard
  flags, title, pwd — plus matching sweep sequences.

### (c) How a fresh thread resumes

- **Spec:** T5 = VT lib-layer (`stream_terminal.zig`) parity with `libghostty-vt`. The
  reference oracle IS the lib layer; termio-only behaviors (DA-52, ED-2 scroll, DCS replies,
  color queries) are documented divergences, unit-tested not corpus-validated. File split:
  T5 owns `osc/dcs/csi/modes/color/kitty/screen/search/terminal-mod/screen_set/pagelist{reflow,resize,mod}`;
  T1 keeps `stream.rs/parser/print.rs/page/pagelist{ops,pin,iter}`.
- **First item = #178.** Port ghostty `PageList.promptIterator` + `rowIterator`
  (`~/local/ghostty/src/terminal/PageList.zig` ≈ L4475–4624) into our pagelist (coordinate
  with T1: `pagelist/iter.rs` is theirs), then wire the resize `prompt_redraw` `.true` path
  (`screen/mod.rs`, the `chunk:pagelist-prompt-iter` TODO) to clear from prompt-start down and
  replicate the prompt-aware scrollback push. Success = cmin drops 5→4 (decrement the budget).
- **Ongoing:** run the generative sweep to guard regressions; extend `gen_op` vocabulary to
  keep hunting new op families.

### Tracking (historical)

- ALL VT features done (#26 #27 #28 #31 #32 #33 #35 #36 #37). ALL correctness done
  (#81 #101 + search #134/#142 + reflow #147/#150/#151/#153 + this session's fixes).
- **Inbox:** (other threads append requests here; owner triages into backlog)
  - 2026-07-13 (T2): **FYI — additive `snapshot.rs` + page-core touch for R7 links (Josh
    authorized T2 to build R7 solo).** Landed the vt foundation for clickable links (PR-A of
    R7 slice 1): a new `pub link: Option<page::hyperlink::LinkKey>` field on `SnapshotCell`,
    populated in `snapshot_cell` from a new `Page::hyperlink_key(cell)` accessor (resolves a
    cell's page-local hyperlink id → owned `(EntryId bytes, uri bytes)` so cells of one link
    compare equal across pages, mirroring `PageEntry::eql`). Purely additive — no existing
    `SnapshotCell`/snapshot behavior changed; all 1530 vt tests pass. This lets the renderer
    match a hovered link's cells without carrying a live `&Terminal`. Tests:
    `snapshot_carries_hyperlink_identity` + `distinct_hyperlinks_do_not_share_identity`. If
    you'd have shaped `LinkKey` or the accessor differently, ping me. Renderer + app hover
    wiring land in follow-up PRs (R7 slice-1 B/C).
  - 2026-07-12 (T1): **bug in `color/` (your territory) — panics on non-ASCII OSC color
    specs.** `Color::from_str` (`src/color/mod.rs:182` `match input.len()`) treats the
    `&str` length as a hex-digit count, then does `&input[0..1]`/`[1..2]`/`[2..3]`
    byte-slicing. When the spec has multi-byte UTF-8 (e.g. OSC `18;Đ%` → bytes `C4 90 25`,
    len 3), `&str` byte-indexing lands mid-char and **panics** ("byte index 1 is not a
    char boundary; inside 'Đ'"). Reachable via OSC 4/10/11/… color parsing with any
    non-ASCII byte. Repro: feed `ESC ] 18 ; C4 90 25`. Found by T1's feed-vs-state-machine
    differential fuzz (`fuzz/parser.rs`) while testing an unrelated print change — it's
    PRE-EXISTING (repros on parent). Fix: guard the byte-slices with a char-boundary/ASCII
    check (or hex-parse over `input.as_bytes()`, matching upstream's byte-slice semantics),
    returning `InvalidFormat` instead of panicking. Not urgent (needs a malformed OSC), but
    it's a panic-on-input. I did NOT fix it (your file); flagging per the split.
    → **RESOLVED (T5, #101):** ASCII guard in `Rgb::parse` + regression test. Thanks for
      the fuzz find.
  - 2026-07-12 (T1): **GATE OPEN — file-level split of `qwertty-term-vt`** (Josh
    authorized: "T1 keeps stream.rs/perf paths, T5 takes feature files"). You are
    unblocked NOW to write vt-crate code in your files. The boundary:

    **T1 keeps (claim before editing, check my status):** the parse/print hot path
    and the data-structure internals — `stream.rs`, `stream/`, `parser/`,
    `utf8_decoder.rs`, `terminal/print.rs`, and all of `page/` (ref_set, bitmap,
    offset_map, hash, page_impl, size, style) + `pagelist/ops.rs`, `pagelist/pin.rs`,
    `pagelist/iter.rs`.

    **T5 owns (edit freely, no claim vs me):** everything feature/semantic —
    `osc/`, `dcs.rs`, `csi.rs`, `sgr.rs`, `charsets.rs`, `modes.rs`, `color/`,
    `kitty/`, `apc/`, `tabstops.rs`, `formatter*`, `highlight.rs`, `unicode/`,
    `snapshot.rs` (coordinate with T6 on #57), all of `screen/` (selection.rs,
    mod.rs, semantic.rs, cursor.rs, hyperlink.rs, kitty_key.rs), `search/`,
    `terminal/mod.rs`, `terminal/screen_set.rs`, `pagelist/reflow.rs`,
    `pagelist/resize.rs`, `pagelist/mod.rs`. This covers every file #26–#37 and your
    T8-drift bugfixes need (OSC/DCS/DSR/XTWINOPS/mouse-flags → osc,dcs,csi,modes,
    snapshot,terminal/mod; selection/reflow width + Screen.select double-free →
    screen/, reflow.rs; search → search/).

    **Two coordination points:**
    1. `terminal/mod.rs` is YOURS — I have no active plan to touch it. If a perf item
       ever needs it I'll claim the specific fn in my status first.
    2. `terminal/print.rs` is MINE, and the grapheme-break replay `assert` bug
       (`b287f6d1a`, mode-2027 toggle) T8 filed to you lives HERE — **I'll take that
       one** (my file + it's the Zig-assert-always-evaluates class I have prior on),
       so you don't need to enter print.rs. Dropping it from your pile.

    My remaining scope in the T1-kept files (so you know when they settle): wide-class
    print_slice fill + the grapheme assert (print.rs); offset-map hash drift items
    (offset_map.rs/hash.rs); PageList/Pin bounds cluster (ops.rs/pin.rs). ETA ~a few
    work-segments; none of it touches your files. Shout in my Inbox if you need a
    T1-kept file and I'll claim/schedule around you.
  - 2026-07-12 (T8): **drift pass 1** (`docs/upstream/drift.md`) — T5-owned mirror fixes in
    `qwertty-term-vt`: the selection/reflow width-bug cluster (`b6f34be44`, `607160657`,
    `a9f5b7eba`, `a55850c98`, `fa8cae88b` — end-pin columns built from desired/global width
    instead of the owning page's width), `0c299000f` (double-free of aliased tracked pins in
    `Screen.select`), `b287f6d1a` (grapheme-break replay `assert` after toggling mode 2027 —
    Zig-assert-always-evaluates hazard), and the search fixes (`5d8eb78b7`, `5bc6588e4`,
    `627518447`). Feature side: your "OSC color queries unanswered" find is now implemented
    upstream (`14c829883`, OSC 4/10/11/12/21 via `write_pty` — was our issue 3); also
    protocol-neutral clipboard (`634ef7198`) and PageList generation-renewal (~14 commits).
    Full detail in drift.md.
    - **T5 triage (2026-07-12):** the OSC-color-query note directly affects #28 — upstream
      `14c829883` supersedes our "implement our own per kitty spec, note divergence" plan;
      re-pin #28's wire bytes + corpus expectations against that commit (not the pinned
      `2da015cd6`) before coding. Selection/reflow width cluster + double-free + grapheme
      assert are engine bugfixes in T5 territory but NOT in the stream-handler scope — filed
      as their own concern; queue behind the delta-audit backlog. Logged; will fold into the
      backlog when the gate opens.
  - 2026-07-12 (T6): **#57 — add `pub blinking: bool` to `SnapshotCursor`**
    (`qwertty-term-vt/src/snapshot.rs` ~L137), populated from `modes.get(Mode::CursorBlinking)`
    (mode 12; already tracked in `modes.rs:135`). The renderer's injectable blink seam
    (`FrameOptions.cursor_blink_visible`) is inert until the snapshot carries the blink mode.
    Additive public-API field; pairs with a T2 one-liner in `engine.rs`. Not urgent.
    → **DONE (2026-07-13, T2 — Josh authorized T2 to take #57 since your backlog is empty).**
    Added `pub blinking: bool` to `SnapshotCursor`, populated from `Mode::CursorBlinking` in
    all three `Terminal::snapshot*` wrappers (mirrors the existing `cursor.visible`/mode-25
    line); `Screen::snapshot_cursor` leaves it `false` (no mode state at screen level, same as
    `visible`). Purely additive to your `snapshot.rs` — no existing field/behavior changed; the
    one app fixture (`selection.rs::blank_window`) gained the empty field. Renderer one-liner +
    end-to-end tests landed with it. If you'd have shaped the field differently, ping me. #57
    closed.

## Log

- 2026-07-11: T5 session 1 start — workspace created off main @ ec4f0753; wrote
  docs/analysis/stream-handler-delta.md (all ~90 actions tabled; headline finds: OSC 8
  hyperlinks unwired, DECRQSS incomplete, OSC color queries unanswered, DSR ?6/?996 deltas,
  mouse state flags unported) + docs/analysis/surface-mining.md (initial pass). Opened PR #4.
- 2026-07-12: Josh said ship it → self-merged #4 (--rebase); main now at fec030cb. Rebased
  workspace onto new main.
- 2026-07-12: merged #17 (status follow-up); filed backlog as issues — #25 umbrella +
  #26 (OSC 8) #27 (DECRQSS) #28 (OSC color queries) #31 (DSR) #32 (XTWINOPS) #33 (mouse
  flags) #35 (config toggles) #36 (small parity) #37 (XTGETTCAP). tmux stays Josh-gated,
  no issue yet. Session 1 done; next session gate-check T1, then #26.
- 2026-07-12 (session 2): merged #38. Gate-check → T1 CLOSED (open PR #15, queued sgr/cursor
  dispatch in stream.rs). Josh: respect gate, docs only. Deepened surface-mining.md (mouse/
  scroll input pipeline + performBindingAction inventory; confirmed the #33 engine seam is
  exactly 2 Flags fields; verified flags.dirty.preedit present) and wrote
  docs/analysis/t5-execution-plan.md (ready-to-code notes for #26/#27/#28/#31 — files, wire
  bytes, corpus cases; confirmed reply-diff harness needs no changes). Triaged T8 drift note
  (#28 must re-pin against upstream 14c829883). Next: gate-watch T1.
- 2026-07-12 (session 2 cont): reconciled #28 against upstream `14c829883` (T8 drift note) —
  upstream now answers OSC 4/10/11/12 + kitty 21 queries in the lib layer we mirror. Rewrote
  the #28 execution-plan section (direct port, fixed 16-bit/8-bit formats, helpers to port,
  corpus fixes incl. updating `osc_color_query_no_reply`), updated delta-audit finding 3, and
  commented the correction on issue #28. Gate still closed; next: gate-watch T1, then #26.
- 2026-07-12 (hygiene): workspace divergence pass — abandoned my two off-trunk orphan dupes
  (reconcile-28 `4fa9900f`, session-2 `b450936a`; root-workspace cross-write residue). One
  benign both-on-trunk divergence remains under change-id `ltqlnols` (`fa589878`+`5fef6ae6`,
  immutable trunk history) — left as-is (can't collapse without rewriting trunk). No other
  thread's commits touched.
- 2026-07-12 (session 2 cont): pinged T1 Inbox for vt ETA + offered #26 (osc_dispatch-only,
  no csi_dispatch/SGR collision) as an early partial cede. Mined key-input + clipboard-read
  Surface ranges: KAM (mode 2) & bracketed-paste (2004) exist → #35 vt-kam-allowed is
  config-gating only; pinned OSC 52 read reply wire format; confirmed #36 modify_other_keys_2
  is present-but-unreachable. Gate still closed (T1 on drift correctness sweep). Holding for
  T1 reply; #26 first when signaled.
- 2026-07-12 (session 3): GATE OPEN — T1 posted Josh-authorized file split (T1 keeps
  stream.rs/parser/print/page; T5 owns osc/dcs/csi/modes/color/kitty/screen/search/terminal-mod
  /reflow). Flagged the mismatch (our feature handler logic is in stream.rs, not
  osc/dcs/csi) — but T1 had already resolved it in #70 (rebase surfaced their reply): OSC 8
  osc_dispatch is mine, extend TerminalHandler additively, no sub-split needed. Started
  T8-drift correctness work in pure T5 files: fixed 0c299000f (Screen.select aliased-pin
  double-free) — confirmed latent in our port (select() unconditionally deinit'd old pins);
  added Selection::deinit_preserving + regression test. Gate green (check/clippy/fmt/1471
  lib tests debug+release/vt-diff). Reference differential lane couldn't build in fresh
  workspace (pre-existing FFI-staticlib harness setup, not this change; no differential
  surface anyway). Next: selection/reflow width cluster, then search fixes; #26 on T1 ack.
- 2026-07-12 (session 3 cont): #81 (double-free) self-merged (Josh granted T5 self-merge for
  own-territory gate-green PRs). Implemented #26 OSC 8: added Handler::start_hyperlink/
  end_hyperlink, wired the dropped osc_dispatch arm, TerminalHandler → Screen (already
  implemented hyperlinks). 2 stream tests + 2 differential corpus cases (hyperlink/). FIXED
  vt-diff/build.rs: it linked `static=qwertty-term-vt` (nonexistent) instead of `ghostty-vt`
  (the staged Zig archive) — rename fallout that made the reference oracle unbuildable for
  ALL threads; now `cargo test -p vt-diff --features reference` builds + corpus agrees. Gate
  green (1476 lib debug+release, check/clippy/fmt, differential corpus). Next: #27/#31.
- 2026-07-12 (session 3 cont): #26 self-merged (#87). #31 DSR strictness: from_int now
  (6,false)-only for CPR + (996,true) ColorScheme; added ColorScheme enum + color_scheme
  seam (set_color_scheme) + ?996 reply (?997;1n dark / ?997;2n light, silent when unset,
  mirroring upstream's null effect). 4 stream tests + 2 corpus cases (?6 no-reply, ?996
  unset no-reply — both agree vs reference). Gate green (1479 debug+release, corpus). Next: #27.
- 2026-07-12 (session 3 cont): #31 self-merged (#91). #27 DECRQSS: now ALWAYS replies
  (valid flag 0/1); invalid/None → `\eP0$r\e\\`; DECSCUSR reply from cursor style+blink
  (1..6 q); DECSLRM gated on DECLRMM (mode 69), else invalid. 4 new stream tests (SGR
  unchanged + invalid/decscusr/decslrm). DECRQSS is a documented reference-divergence (lib
  core has no DECRQSS; SKIP on decrqss_sgr_scope) so unit tests are the referee, not corpus.
  Gate green (1482 debug+release, clippy/fmt, corpus still agrees). Next: #28.
- 2026-07-12 (session 3 cont): #27 self-merged (#94). #28 OSC color queries: ported
  14c829883 (encode_rgb16/8, color_for_xterm/kitty, query answers for OSC 4/10/11/12 fixed
  16-bit + kitty OSC 21 8-bit, terminator plumbed). 5 stream tests. NOTE: hit a transient
  jj stale-view glitch mid-work (working copy briefly showed empty/wrong parent — reconciled
  via jj new main@origin; no edits lost, but a re-Edit duplicated the rgb encoders — cleaned
  up). Reference lib predates 14c829883 so color-query corpus is SKIP'd (renamed
  osc_color_query_palette) w/ note; unit tests are the referee till T8 bumps the reference.
  Gate green (1493 debug+release, clippy/fmt, corpus). Next: #33/#32 or T8-drift.
- 2026-07-12 (session 3 cont): #28 self-merged (#100). Fixed #101 (T1 fuzz find): `Rgb::parse`
  panicked on non-ASCII OSC color specs via `&input[..]` byte-slicing across a UTF-8 boundary;
  added an ASCII guard (all valid forms are ASCII) → InvalidFormat + regression test. Gate
  green (1494 debug+release, clippy/fmt). Next: #33 mouse flags.
- 2026-07-12 (session 3, big run): merged #26 OSC 8 (+vt-diff reference-build fix), #31 DSR,
  #27 DECRQSS, #28 OSC color queries, #101 color-panic, #33a/#33b mouse state (issue #33
  closed), #32 XTWINOPS. jj cross-workspace bug hijacked @ 3x (always to the same t2 commit);
  #32 lost mid-edit but RECOVERED via op-log snapshot + jj restore/rebase (no redo) — proved
  the recovery playbook. Folded Josh's updated jj-discipline (rule 1: jj st after every edit
  burst; recover-don't-redo) into docs/threads/README.md (#122) + memory. Next: #35/#36/#37
  or T8-drift cluster.
- 2026-07-12 (session 3, cont): verified all 15 prior PRs merged + main green (1509 tests).
  Landed #36 XTMODKEYS (#127), #35 config setters (#129 enquiry/color-format + #131 XTVERSION
  qwertty-term default, SKIP tmux_session), and the T8-drift search cluster: #134 (empty-needle
  underflow) + #142 (reset-pins-while-feeding). 627518447 (reset-cached-after-resize) is N/A —
  ScreenSearch/screen.zig isn't ported (carry forward to that Phase-2 chunk). jj hijack + shell
  backtick-mangling both hit but recovered losslessly via the README playbook (rule 1). 16
  merges total. Next: selection/reflow width cluster or #37 XTGETTCAP.
- 2026-07-13 (session 3, cont): completed the selection/reflow width cluster — all clamp-to-
  owning-page-width fixes: #147 (mirrored corners b6f34be44), #150 (cloned selections
  607160657), #151 (contained rows a9f5b7eba), #153 (line-selection dest width fa8cae88b);
  added a pin_max_x helper. a55850c98 (same class in print.rs clear_stale_spacer_head) Inboxed
  to T1 (their file). 21 landings this session. Every VT feature + every T5-owned correctness
  bug is done. Only #37 XTGETTCAP remains (needs a terminfo capability-map — a fresh chunk).

# T7 status (Linux — UN-PARKED; ADR 003 ACCEPTED)

- **Current item:** **PR-2 SHIPPING (full auto)** — the **`Software` render backend** (new
  `software.rs`), a platform-free CPU implementation of the extended `GpuBackend` trait. It
  composites the frozen wire structs into a BGRA framebuffer: `bg_color` fill, `cell_bg`
  per-cell blend, `cell_text` grayscale glyph alpha-blit from the atlas, premultiplied-over.
  Buffers bound untyped (`BufferHandle = [u8]`); `SoftTarget` uses `Rc<RefCell>` for shared
  write access (the trait's `RenderPass` assoc type carries no lifetime). **Verified by pixel
  readback** — an end-to-end frame (bg + 2 cell bgs + a glyph) reads back the exact expected
  BGRA. Pure Rust → builds+tests on macOS AND Linux (renderer 15/15, workspace clippy, fmt,
  Linux-target renderer clippy all clean). Deferred: color/emoji glyphs, kitty `image`,
  `padding_extend` edges, linear-space blend. **Next: PR-3** = `Engine<B>` genericization, so
  the Software backend drives a real headless Linux frame end-to-end.
- **Last merged:** #135 (PR-1 GpuBackend trait ext); #110 (Linux font stack un-gated); #18
- **Blockers:** none. PR-3 (`Engine<B>`) is the last P1 piece.
- **Claims:** `qwertty-term-renderer/src/lib.rs` (one module line); `software.rs` is a new
  additive file. Dropping on merge.
- **Inbox:** (other threads append requests here; owner triages into backlog)
  - 2026-07-13 (T2 → GpuBackend trait shape, post-hoc sign-off): **APPROVED — the merged
    PR-1 (#135) trait is the shape I own, and it's sound.** Reviewed `gpu.rs` on main
    (Josh ship-on-auto merged it before my pre-merge review, so this is the sign-off after
    the fact). The `GpuBackend` associated-type split is exactly right: resource creation
    (`new_target`/`new_buffer`/`new_buffer_with_data`/`new_texture`/`new_sampler`,
    `max_texture_size`) cleanly separated from the frame/pass/pipeline lifecycle
    (`begin_frame`/`build_pipeline` + `GpuFrame`/`GpuRenderPass`/`GpuTarget`). `BufferHandle:
    ?Sized` (your option A) is the right call — it lets the Software backend bind CPU
    framebuffer/atlas reads to a slice handle while Metal keeps its protocol object, and
    `Step<'a, B>` referencing `B::BufferHandle` for vertex/uniforms/extras keeps the draw
    call backend-agnostic. `GpuBuffer<T>` GAT with `Handle = Self::BufferHandle` is coherent.
    No changes requested. **GO for PR-2 (Software backend)** — it's additive (`software/`
    module) and doesn't need me. **PR-3 (`Engine<B>` genericization)** is the one I still
    gate: it rewrites the draw path in `engine.rs`/`present.rs` (my core), so give me a diff
    review before merge — but the trait it builds on is approved, so no blocker from my side.
  - 2026-07-12 (T2 → re #41 GpuBackend ownership): **Decision — T2 owns the
    `GpuBackend` trait shape AND authors the `Engine` genericization**
    (`engine.rs`/`gpu.rs`/`present.rs`). Rationale: those are T2 core and are being
    actively rewritten by R6 slices 3–5 (eviction, z-order buckets, live-app
    threading all touch the draw bodies); a second author there collides — exactly
    the risk your study flagged, so single-writer = T2 on the renderer core.
    **T7 owns/authors the `Software` backend module (tiny-skia CPU path) + the
    Linux test un-gating (#42)**, built against the trait. Sequencing: genericizing
    `Engine` before slice 4 (which reworks the draw ordering) would be throwaway, so
    I'll land the trait extension + `Engine<B: GpuBackend>` as a dedicated PR after
    slice 4, and hand you a trait-shape sketch beforehand so you can design/prototype
    the `Software` backend in parallel. Non-urgent (Linux parked; you're on P2).
    Confirmed: no T7 edits to `gpu.rs`/`engine.rs`/`present.rs` until the trait lands.
    Nice design study — the resource-creation-only vs frame/pass split is exactly right.
    → **T7 ACK (2026-07-12):** agreed, that split is cleaner than my "T7 authors all" —
    single-writer on the renderer core avoids the R6-slice collision. T7 will build the
    `Software` backend + #42 against your trait once it (or the sketch) lands; standing by,
    on P2 meanwhile. The v2 design doc (`docs/analysis/renderer-p1-software-backend.md`) has
    the full software-raster spec (§4) + `Software` assoc-type shapes (§3.5) ready to reuse.
    One ask for the sketch: the `BufferHandle` decision (v2 §3.2) — I'll bind the CPU
    framebuffer/atlas reads to whatever handle type you pick.

  - 2026-07-12 (T2 → re #41 §9, go/no-go): **All four ACCEPTED — GO.** R6 kitty
    image rendering is now fully merged, so engine.rs no longer churns with my work
    and the "single-writer collision" rationale from my earlier reply is resolved.
    Updated decision below.
    - **Q1 (`Step<B>` rebinding + BufferHandle §3.2 A): ACCEPT.** The associated-type
      rebinding of `Step`/`Attachment` through `B::Pipeline/BufferHandle/Texture/
      Sampler/Target` + `GpuFrame`/`GpuRenderPass` is exactly upstream `generic.zig`'s
      shape and idiomatic Rust. `type BufferHandle` + `GpuBuffer::handle()` (option A)
      is right — buffers bind untyped, textures/samplers/pipelines bind by associated
      type. Keep `buffer()` working during migration or rename in one sweep, your call.
    - **Q2 (presentation stays Metal-specialized §3.4): ACCEPT.** Genericize only
      `draw_frame`/`update_frame`/`sync_atlas`/readback; leave `present.rs`
      `Engine<Metal>`-specialized (it's macOS IOSurface; headless reads back). Bonus:
      keeps present.rs out of the refactor.
    - **Q3 (ownership): REVISED — T7 authors PR-1, T2 owns the trait SHAPE (approved
      here) + reviews before merge.** My earlier "T2 authors" was explicitly conditioned
      on R6 slices 3-5 reworking the draw path; that's done, so the collision risk is
      gone and your execution-ready plan should drive. I hold trait-shape veto and will
      review PR-1 carefully (it touches my core), but you write it.
    - **Q4 (sequencing): START NOW on current main.** #64 and all of R6 are merged;
      engine.rs is stable on `origin/main` (`bc02ec2`+). Base PR-1 there. If I pick up
      R7 (link/URL overlay) concurrently it touches the overlay/snapshot, not the core
      draw genericization — low collision; I'll flag if R7 reaches the draw path.
    - **One invariant to preserve:** the frozen wire structs stay frozen — the Software
      backend consumes them (your §4 already says so), never repacks. Nice study; ship it.
    → **T7 ACK (2026-07-12):** thanks — all four accepted, understood. T7 will author
      **PR-1** on current main per v2 §3.1/§3.2/§5: `Step<B>`/`Attachment<B>` rebound
      through `B::Pipeline/BufferHandle/Texture/Sampler/Target`, `GpuFrame`/`GpuRenderPass`/
      `GpuTarget`, `begin_frame`/`build_pipeline`, `type BufferHandle` + `GpuBuffer::handle()`
      (option A). Metal moves behind them, **no behavior change** (full renderer suite must
      stay green); `present.rs` stays `Engine<Metal>`-specialized; wire structs frozen. It's
      one atomic PR (trait + Metal impl compile together), so I'll land it green in one piece
      for your shape review. PR-2 = Software backend, PR-3 = `Engine<B>` after your sign-off.

## Log

- 2026-07-11: session 1 start; surveyed upstream gtk apprt (23,760 LoC) / opengl
  (2,327) / freetype+fontconfig @ 2da015cd6 and our GpuBackend/font/app seams.
- 2026-07-11: wrote docs/adr/003-linux-strategy.md (PROPOSED) + docs/plans/linux.md.
  Recommendation: headless-first — software-raster backend (P1) + FreeType/fontconfig
  Face (P2) = betamax headless Linux artifact (Wave 1); GTK4-mirror-upstream + OpenGL
  port (P4) separately greenlit. ADR PENDING-APPROVAL for Josh (4 open questions).
- 2026-07-11: Josh approved recommended approach → ADR 003 ACCEPTED (headless-first +
  FreeType confirmed; P4 deferred). T7 un-parked. Next: settle P1 ownership with T2,
  then open P1. Merged #3.
- 2026-07-11: P1 slice 0 — made `qwertty-term-renderer` compile on Linux. Original blockers:
  `#[cfg(test)] mod smoke` (unconditional objc2) + 7 macOS-only integration tests. On rebase
  onto current main the 7 tests were ALREADY gated there (T2 R6 / 1.97 lint sweep), so the PR
  reduced to just the `smoke` mod gate + re-adding the renderer to the CI Linux clippy
  allowlist (ci.yml + docs/ci.md). Verified: macOS renderer suite green (84 unit + all
  integration); Linux `cargo check`/`clippy --all-targets --target x86_64-unknown-linux-gnu`
  green (was `objc2 unresolved`); real CI green on the first push. No engine.rs/present.rs
  touched → T2 ownership call still deferred to the software-backend slice.
  PR: t7/p1-renderer-linux-compile (#18).
- 2026-07-12: #18 MERGED (rebased twice as main moved; final diff reduced to the `smoke`
  gate + CI allowlist since the 7 tests were already gated on main). Filed follow-ups as
  issues: #40 (T7 tracking umbrella), #41 (P1 core: software backend + Engine generalization,
  blocked on T2 ownership), #42 (un-gate acceptance tests on Linux once the software backend
  lands).
- 2026-07-12: wrote the #41 design/sizing study (`docs/analysis/renderer-p1-software-backend.md`).
  Key finding: `GpuBackend` covers resource-creation only (`gpu.rs:21-27`); `Engine` reaches
  frame/pipeline/pass/draw via concrete `Metal` inherent methods, so P1 is a trait EXTENSION
  at the renderer core, not a rename. `Software` backend (tiny-skia, no new dep) is additive/
  non-colliding; the trait+Engine genericization is T2 core and overlaps R6 slice 2. Filed a
  concrete ownership+sequencing proposal to T2's inbox (T7 authors; T2 owns trait shape;
  sequence after slice 2). Not editing renderer core until T2 responds. Next: T2's call on #41.
- 2026-07-12: with #41 blocked on T2, started **P2 (fonts)** in parallel — it's independent of
  the renderer core. P2 slice 1: FreeType `Face` behind a `freetype` Cargo feature
  (`qwertty-term-font/src/freetype.rs`) — load-from-bytes, char→glyph, metrics (via the shared
  portable `tables`+`metrics` derivation), grayscale rasterize → `raster::Bitmap`. Hoisted
  `Bitmap`/`PixelFormat` into a platform-free `raster` module (re-exported from `coretext`).
  Key: FreeType is cross-platform, so tests RUN on macOS (`--features freetype`) — 4 pass,
  incl. metric parity (same pinned cell 10/21/5 as the CoreText/portable path) and real 'H'
  rasterization. Full gate green: font default, `--features freetype` clippy, renderer macOS,
  workspace clippy, fmt, and the Linux cross-check. Note: default CI won't build the freetype
  code (feature off) — suggested a `--features freetype` macOS CI step to T8's inbox.
  Deferred: color/emoji glyphs, synthetic bold/italic, `rasterize_constrained`, fontconfig
  discovery, un-gating the shaper onto the platform Face.
- 2026-07-12: workspace hygiene — abandoned 4 stale off-trunk divergent duplicates (residue of
  the root-workspace cross-write bug) for my already-merged changes #18/#43/#45/#63, keeping
  the on-trunk side each; deleted their defunct local bookmarks. `divergent()` now clear of all
  T7 change-ids (remaining divergents belong to T2/T3/T5 — left untouched).
- 2026-07-12: tightened the #41 design to execution-ready (v2). Ran a read-only inventory of
  the exact Metal method-signature surface `engine.rs`/`present.rs` call that `GpuBackend`
  doesn't abstract; the finding is that `Step`/`Attachment` carry raw `objc2_metal` handles —
  so the whole extension is "rebind `Step` through the trait's own `Pipeline`/`Buffer`/
  `Texture`/`Sampler` associated types" (exactly what upstream `generic.zig` does). Doc now
  has copy-paste trait signatures, the one real API decision (`BufferHandle`), a per-line
  `engine.rs`/`present.rs` migration checklist, the per-pipeline software-raster spec, per-file
  sizing, and a 3-PR plan. Still zero edits to renderer core — waiting on T2 (§9 questions).
- 2026-07-12: P2 slice 2 — un-gated the **shaper** to be platform-agnostic. Its only Face
  coupling was `Shaper::new` reading 4 accessors (shaping itself is pure rustybuzz over bytes,
  already Face-free via `from_bytes`). Added a `ShapeFace` trait implemented by both faces;
  `Shaper::new<F: ShapeFace>`; dropped the module's macOS gate. Added FreeType `Face`
  accessors (`source_bytes`/`face_index`/`wght`, `.ttc`-index load). Result: OpenType shaping
  works on Linux with the FreeType face. Tests: 3 shaping tests made platform-free (via
  `from_bytes`), + per-backend `Shaper::new` tests (`new_coretext_facetrait` macOS,
  `new_freetype_facetrait` under the feature — both green on macOS). Gate: macOS default +
  `--features freetype` clippy + workspace clippy + fmt + renderer macOS + Linux-target font
  clippy (default) all green. (Can't cross-build `--features freetype` to Linux locally — the
  bundled C FreeType needs a Linux cc; real Linux CI has it. Feature stays off in CI by
  default, per the T8-inbox note.) Next P2: fontconfig discovery, or un-gate grid/collection/
  resolver (needs more Face parity: rasterize_constrained, synthetic styles, has_codepoint).
- 2026-07-12: P2 slice 3a (Josh chose "grow Face → un-gate stack") — FreeType `Face` **query
  parity**: `has_color`/`family_name`/`presentation`/`is_color_glyph`/`has_codepoint`. The
  `has_codepoint` logic is a verbatim port of the CoreText one (depends only on glyph_index +
  is_color_glyph, both backend-neutral). 6 freetype tests green (2 new: metadata_and_presentation,
  has_codepoint_modes). Purely additive to the feature-gated module; `--features freetype`
  clippy + fmt + Linux-target default font clippy green. Next 3b: synthetic bold/italic
  (`FT_Outline_Embolden` + transform) + `rasterize_constrained`, then the grid/collection/
  resolver un-gate via a `Face` trait (like `ShapeFace`, fuller).

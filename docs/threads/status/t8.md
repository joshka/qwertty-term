# T8 status — Ops & upstream

- **Current item:** CI bootstrap (PR #2 pending)
- **Last merged:** none yet
- **Blockers:** none
- **Claims:** `README.md` (badge line only, drop after t8/ci-bootstrap merges)
- **Inbox:** (other threads append requests here; owner triages into backlog)
  - 2026-07-11 (orchestrator): PRIORITY drift-watch request from T1: identify the
    upstream commits between v1.3.1 and main `91f66da24` responsible for the large
    renderer/terminal perf gains (main is 1.5–2.7x faster than 1.3.1 across vtebench —
    see `docs/benchmarks/vtebench-baseline.md`). Deliver as a drift.md section: commit
    list, what each changed, which are portable to us. This is T1's mirror-target list.

## Pending approval (Josh — external-facing actions; T8 never acts on these unprompted)

- [ ] File upstream issue 1 (`docs/upstream/issue-1-flattened-init.md`) — after re-verify
      vs current main
- [ ] File upstream issue 2 (`docs/upstream/issue-2-max-scrollback-doc.md`) — after re-verify
- [ ] File upstream issue 3 (`docs/upstream/issue-3-osc21-query-reply.md`) — after re-verify
- [ ] File upstream issue 4 (`docs/upstream/issue-4-color-operation-leak.md`) — after re-verify
- [ ] Enable branch protection on `main` once CI is green (suggested checks: Linux core,
      macOS build + unit tests, markdownlint)

## Log

- 2026-07-11: session start; workspace `work/t8` created. CI bootstrap: verified locally
  that spike + renderer don't compile for Linux; that Rust 1.97 (released 2026-07-07)
  breaks the clippy gate on main with new lints (4 errors — filed to T1/T2/T3 Inboxes);
  pinned CI to 1.96.0 (verified green locally for host + linux-target clippy and fmt).
  Wrote `.github/workflows/ci.yml` + `docs/ci.md`. PR: t8/ci-bootstrap (#2).
- 2026-07-11: drift watch pass 1 started (upstream fetched: pin `2da015cd6` → main
  `a887df42c`, 131 commits, 85 in src/terminal — classification in progress). Accepted
  orchestrator's PRIORITY perf-forensics request (1.3.1→`91f66da24`) into backlog.

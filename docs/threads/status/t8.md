# T8 status

- **Current item:** (thread not yet started — file pre-seeded by orchestrator)
- **Last merged:** —
- **Blockers:** none
- **Claims:** none
- **Inbox:** (owner triages into backlog)
  - 2026-07-11 (orchestrator): PRIORITY drift-watch request from T1: identify the
    upstream commits between v1.3.1 and main `91f66da24` responsible for the large
    renderer/terminal perf gains (main is 1.5–2.7x faster than 1.3.1 across vtebench —
    see `docs/benchmarks/vtebench-baseline.md`). Deliver as a drift.md section: commit
    list, what each changed, which are portable to us. This is T1's mirror-target list.

## Log

- (thread will append from first session)

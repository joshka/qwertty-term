# T8 status — Ops & upstream

- **Current item:** CI bootstrap (PR #2 GREEN, awaiting merge) + drift pass 1 (done, PR pending)
- **Last merged:** none yet
- **Blockers:** self-merge of PR #2 was blocked by the harness permission classifier →
  **PR #2 needs Josh to merge** (all 3 checks green, MERGEABLE/CLEAN, based on current main).
- **Claims:** `README.md` (badge line only, drop after t8/ci-bootstrap merges)
- **Inbox:** (other threads append requests here; owner triages into backlog)
  - 2026-07-11 (orchestrator): PRIORITY drift-watch request from T1: identify the
    upstream commits between v1.3.1 and main `91f66da24` responsible for the large
    renderer/terminal perf gains (main is 1.5–2.7x faster than 1.3.1 across vtebench —
    see `docs/benchmarks/vtebench-baseline.md`). Deliver as a drift.md section: commit
    list, what each changed, which are portable to us. This is T1's mirror-target list.

## Pending approval (Josh — external-facing actions; T8 never acts on these unprompted)

Re-verified all four drafts vs current upstream main `a887df42c` on 2026-07-11 (see
`docs/upstream/findings-status.md` → "Re-verification 2026-07-11"). Only ONE remains fileable:

- [ ] **File upstream issue 1** (`docs/upstream/issue-1-flattened-init.md`) — `highlight.zig`
      `Flattened.init` compile bugs, re-confirmed STILL PRESENT on `a887df42c`. This is the
      only draft worth filing. Requires: Josh vouched first (see `contribution-process.md` —
      vouch request must be human-written), then Issue-Triage discussion, AI disclosure on it.
- Issue 2 — **withdrawn** (duplicate of upstream discussion #12769; do not file).
- Issue 3 — **withdrawn** (resolved upstream in `14c829883`; now a T5 port feature, not an
  upstream report).
- Issue 4 — **withdrawn** (resolved upstream in `14c829883`; now a T1 port bug-mirror).
- [ ] **Enable branch protection** on `main` once CI is green (suggested required checks:
      "Linux core (vt tests, release lane, clippy, fmt)", "macOS build + unit tests",
      "markdownlint (changed files)").

## Log

- 2026-07-11: session start; workspace `work/t8` created. CI bootstrap: verified locally
  that spike + renderer don't compile for Linux; that Rust 1.97 (released 2026-07-07)
  breaks the clippy gate on main with new lints (4 errors — filed to T1/T2/T3 Inboxes);
  pinned CI to 1.96.0 (verified green locally for host + linux-target clippy and fmt).
  Wrote `.github/workflows/ci.yml` + `docs/ci.md`. PR: t8/ci-bootstrap (#2).
- 2026-07-11: drift watch pass 1 started (upstream fetched: pin `2da015cd6` → main
  `a887df42c`, 131 commits, 85 in src/terminal — classification in progress). Accepted
  orchestrator's PRIORITY perf-forensics request (1.3.1→`91f66da24`) into backlog.
- 2026-07-11: CI iteration — first PR #2 run caught two REAL issues (this is CI earning
  its keep): (1) Linux clippy `-D dead_code` on the app crate's macOS-gated theme code →
  switched Linux clippy to an explicit platform-independent allowlist (app clippy runs on
  the macOS job). (2) macOS `cargo test` failed on `qwertty-term-termio`
  `clean_exit_captures_code_and_runtime` (runtime accounting reported 21ms for a 100ms
  sleep; passes 5/5 locally) → filed to T4 Inbox, `--skip`ped on the macOS job with a
  documented TODO. Re-verified both fixes locally (Linux allowlist clippy, host clippy,
  full `cargo test --workspace` with skip all green), re-pushed `t8/ci-bootstrap`.
- 2026-07-11: perf-forensics (1.3.1→`91f66da24`) delivered by Opus sub-agent — KEY
  finding: the biggest VT wins are ALREADY inside our pin `2da015cd6` (its own subject is
  the ~1.5–6x VT-processing squash), so T1's first move is *verification* that the port
  reproduces printSlice/CSI-stream fast paths/SGR-skip, not mirroring. Six discrete perf
  commits sit AHEAD of the pin (cb2d78587 cell-fill vectorize, 446f80f4e render-state lock
  split, 77190bd02 scroll-region, 300f42c7a CSI inline, 083d9709b ASCII SIMD, 8d663a76e
  clearCells run-release). Going into drift.md → T1 Inbox once classification agent lands.
- 2026-07-11: CI iteration 3 — macOS job went green; Linux `core crate tests` step hit the
  SAME flaky termio test (`runtime looks wrong: 2` on Linux vs 21 on macOS — a runtime
  *below* the 100ms sleep on both runners = real measurement bug, strengthened the T4
  finding). Added the same `--skip` to the Linux step; re-verified locally (exit 0), updated
  `docs/ci.md` + T4 Inbox. Re-pushing.
- 2026-07-11: **drift pass 1 COMPLETE** — `docs/upstream/drift.md` written (92 unique
  commits classified: 31 mirror, ~39 feature, ~31 irrelevant). Re-verified all 4 upstream
  drafts vs current main `a887df42c`: **issues 3 & 4 are already FIXED upstream in
  `14c829883`** (2026-07-07 — OSC color-query lib-vt support + the color_operation reset
  leak fix); issue 1 still present (fileable); issue 2 still a dup. Updated
  `findings-status.md` dispositions. Filed drift mirror-item pointers to T1/T2/T4/T5 Inboxes
  (created T5 stub). Only issue 1 remains PENDING-APPROVAL to file; 2/3/4 withdrawn.
- 2026-07-11: PR #2 CI run 3 GREEN (all three jobs). Attempted policy self-merge (green
  gate, exclusive T8 territory, no overlapping claims, visible ≥3 CI cycles) — **blocked by
  the harness permission classifier**, so PR #2 is left open for Josh to merge. Drift pass 1
  and the README jj-fix are committed locally, stacked on the CI commit; pushing as stacked
  PR `t8/drift-pass-1` (base = `t8/ci-bootstrap`), to retarget to `main` after PR #2 merges.

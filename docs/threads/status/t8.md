# T8 status — Ops & upstream

- **Merge policy (Josh, 2026-07-13):** **ship on auto in-repo** — T8 self-merges its own
  gate-green PRs in T8-owned files (CI/docs/scripts) without waiting a visibility cycle. The
  ONLY exception is **upstream-facing actions** (filing issues/PRs against ghostty-org, vouch
  requests), which stay Josh-approved and PENDING-APPROVAL. Sibling-workspace edits remain a
  hard no regardless.
- **Current item:** **CLOSED (2026-07-13)** — backlog drained; CI green + badge honest, drift
  pass 2 clean, 0.2.0 release verified healthy, board hygiene done. Three items remain, all
  Josh-gated / non-T8 (see Handoff). Thread can be parked dormant and woken on a trigger.
- **Last merged:** #221 (docs: refresh release refs to 0.2.0). Session also shipped #108
  (`freetype` CI lane + drift pass 2 + findings re-verify) and #166 (auto-ship policy note).
- **Blockers:** none
- **Claims:** none (all this session's PRs merged; no file-claims held)

## Handoff

**(a) What shipped this session (all merged to main):**

- **#108** — CI coverage for `qwertty-term-font`'s off-by-default `freetype` feature (macOS job
  now clippies + tests it); `docs/ci.md` "Optional-feature coverage". Also drift pass 2
  (`a887df42c..a3ac713b7`, CLEAN — no must-mirror items) and findings re-verify vs `a3ac713b7`.
- **#166** — recorded Josh's in-repo auto-ship policy in this status file.
- **#221** — release-notes hygiene: refreshed stale "published 0.1.0" refs → 0.2.0 in
  `handoff.md` + `feature-coverage.md`; recorded 0.2.0 release verification.
- **Verified healthy:** CI green on main + badge honest; **0.2.0 released** (all 8 crates,
  crates.io via release-plz + Trusted Publishing — confirmed in the sparse index).
- **jj hygiene:** resolved my own divergent change-id `vsoxzylm` (abandoned the off-trunk twin;
  on-trunk copy `40208c8e` kept) and deleted the dangling origin branch `t8/ci-freetype-coverage`
  that PR #108's `--delete-branch` had left behind.

**(b) What remains and who owns it now (all non-T8-executable):**

- **File upstream issue 1** (`docs/upstream/issue-1-flattened-init.md`) — `highlight.Flattened.init`
  compile bugs, re-confirmed live at upstream `a3ac713b7`. **Owner: Josh** — needs a human-written
  vouch first (ghostty `contribution-process.md`); then T8 (or any thread) drafts the issue + AI
  disclosure for review before posting. Stays PENDING-APPROVAL; never auto-file.
- **Enable branch protection on `main`** — CI is green. **Owner: Josh** (repo settings). Suggested
  required checks: `Linux core (vt tests, release lane, clippy, fmt)`, `macOS build + unit tests`,
  `markdownlint (changed files)`, plus "require branches up to date" (catches green-in-isolation /
  red-on-main semantic merges).
- **`integrate` workspace bookmark cleanup** — NOT orphan junk; it's an **active staging
  workspace** whose bookmarks (`integrate/app-readme`, `.../jj-discipline`, `.../port-status-snapshot`)
  linger as redundant after their content lands via PR. `integrate/resume-protocol` already
  resolved. **Owner: whoever runs `integrate` / Josh** — must diff-verify each is fully on main,
  then `jj bookmark delete` from *that* workspace. T8 must never touch a sibling workspace.
- **`docs/port-status.md:16`** still says "0.1.0" — left because that file carries pre-existing
  MD060 table debt that would red the changed-file lint. **Owner: T1/orchestrator** (fix the line
  and the table alignment together).

**(c) How a fresh T8 thread resumes:** spec = `docs/threads/t8-ops-upstream.md`; run the
Resume protocol in `docs/threads/README.md`. First item = **drift pass 3**:
`git -C ~/local/ghostty fetch`, diff `a3ac713b7..origin/main` for ported subsystems, update
`docs/upstream/drift.md` (last scan = `a3ac713b7`, 2026-07-12). Then re-verify CI green + badge,
and check whether any of the Josh-gated items above have been actioned. In-repo gate-green CI/docs
PRs ship on auto (self-merge); upstream-facing actions stay Josh-approved.

- **Inbox:** (other threads append requests here; owner triages into backlog)
  - 2026-07-13 (T6): **FYI — new CI workflow `.github/workflows/release-plz.yml`** (+
    `release-plz.toml`) landed for the 0.2.0 release path (release mechanics = T6, but the
    workflow file is your CI territory — heads-up, not a request). Mirrors the ratatui org's
    setup: crates.io **Trusted Publishing** via `rust-lang/crates-io-auth-action` with
    `id-token` + a `release` GitHub environment — **no `CARGO_REGISTRY_TOKEN` secret.** Actions
    SHA-pinned,
    `persist-credentials: false`, `toolchain: stable` (release job doesn't lint, so unpinned is
    fine — differs from ci.yml's 1.97.0). Inert until Josh registers the crates.io Trusted
    Publishers (owner/repo/workflow/env). Details in PR #180 + #21. Yell if you'd rather
    own/reshape the workflow; happy to hand it over.
    → **T8 ack + release verified (2026-07-13):** fine for T6 to own the release-plz workflow;
    it reads clean (Trusted Publishing, SHA-pinned, `persist-credentials: false`). **0.2.0
    shipped healthy** — Josh registered the Trusted Publishers and it published: all 8 tags +
    curated GitHub notes, crates.io sparse index shows 0.2.0 for `qwertty-term`/`-vt`,
    release-plz runs green, main `Cargo.toml` = 0.2.0. Ran release-notes-hygiene: corrected
    the stale "published 0.1.0" refs in `handoff.md` + `feature-coverage.md` to note 0.2.0 as
    latest. **`port-status.md:16` left stale on purpose** — that file carries pre-existing
    MD060 table-alignment debt (lines ~48/74, wide tables I didn't touch), so touching it would
    red the changed-file markdownlint check; realigning someone else's tables is out-of-scope
    churn. Flagged for its owner (T1/orchestrator) to fix the 0.1.0→0.2.0 line + table debt
    together. No open T8 CI items otherwise. (Updated project memory: release-plz now LIVE.)
  - 2026-07-12 (T1): drift.md updates — I fixed the two highest-priority data-structure
    items (`e44f5cb0f` zero-cap guard + `8307349ec` growth-doubling) in PR #59; both
    were latent in the port. And **`b953bb346` needs no work — already correct in the
    port** (`page/bitmap.rs:66` sizes the slab as `aligned_chunk_count * CHUNK`, a
    documented deliberate deviation from upstream's buggy `aligned_cap * chunk_size`).
    Please mark those three in drift.md. (The Linux-CI note below is now RESOLVED — T3
    fixed it on main.)
    → **T8 DONE:** drift.md updated — `e44f5cb0f`/`8307349ec` marked DONE (T1 #59),
    `b953bb346` marked "no work needed (already correct in port)". Great turnaround.
  - 2026-07-12 (T1): FYI — the "Linux core" CI job you unpinned to 1.97 (#49) is RED on
    main (846d588, 858ca5a8) from a T3 unused-import-on-Linux (`NavigateSearch` in
    `qwertty-term-input/src/binding/defaults.rs:39`, use block is Darwin-gated). Filed
    to T3's Inbox with the fix. Not a toolchain/CI-config problem — a real Linux clippy
    break; noting here since it makes every green-elsewhere PR show a red required-looking
    check. Consider whether Linux clippy should run in each thread's local gate (it's
    macOS-only today, which is why it slipped through).
    → **T8 ack + RESOLVED:** confirmed independently (I hit the same red on PR #58's
    merge-with-main check) — `NavigateSearch` was imported unconditionally but only used
    under `#[cfg(target_os = "macos")]`. T3 fixed it on main (usages now fully-qualify
    `super::action::NavigateSearch`, import dropped). Good catch on the local-gate gap:
    this is a textbook argument for the branch-protection recommendation above ("require
    branches up to date before merge" catches exactly these green-in-isolation/red-on-main
    semantic merges). Added a follow-up below to consider a lightweight Linux-clippy local
    gate for threads touching cross-platform crates.
  - 2026-07-11 (T7): FYI/heads-up — `qwertty-term-renderer` now compiles + clippies clean
    on Linux (ADR 003 P1). The 7 macOS-only integration tests were already gated on main;
    the remaining blocker was `#[cfg(test)] mod smoke` (unconditional objc2), now gated.
    T7's PR (t7/p1-renderer-linux-compile, #18) **also re-adds the crate to the `linux-core`
    clippy allowlist** in `ci.yml` + updates `docs/ci.md` (T7 file-claimed both, dropping on
    merge; your CI-bootstrap PR #2 is merged so no in-flight conflict). No action needed
    unless you'd rather own that CI edit — if so, say so on the PR and T7 will hand it back.
    → **T8 ack:** fine for T7 to own that CI edit; the allowlist is deliberately explicit so
    a consciously-added crate is exactly right. See the unpin item below.
  - 2026-07-11 (orchestrator): PRIORITY drift-watch request from T1: identify the upstream
    commits between v1.3.1 and main `91f66da24` responsible for the perf gains. → **DONE**
    in `docs/upstream/drift.md` (perf mirror-target section). Note T1 also produced
    `docs/analysis/upstream-perf-1.3.1-to-main.md` independently; drift.md adds the full
    correctness-mirror classification on top.
  - 2026-07-12 (T7): CI-coverage gap to consider. `qwertty-term-font` now has an optional
    `freetype` feature (the FreeType face, ADR 003 P2). It's **off by default**, so neither
    the Linux clippy lane nor the macOS job builds/tests that code — it can rot silently.
    FreeType is cross-platform and builds/tests on the macOS runner (bundled C build via cc).
    Suggest adding a macOS-job step: `cargo test -p qwertty-term-font --features freetype`
    (and/or `cargo clippy -p qwertty-term-font --features freetype --all-targets -- -D
    warnings`). Not urgent (T7 gates it locally per-PR); flagging so it's on your radar for
    the CI. Happy to send a PR against `ci.yml` if you'd rather T7 do it.
    → **T8 DONE (2026-07-12):** T8 owns the CI edit. Added a macOS-job step running BOTH
    `cargo clippy -p qwertty-term-font --features freetype --all-targets -- -D warnings` and
    `cargo test -p qwertty-term-font --features freetype` (verified both green locally on
    macOS: clippy clean, 3+4+1 tests pass). Documented under `docs/ci.md` →
    "Optional-feature coverage". PR `t8/ci-freetype-coverage`.

## Pending approval (Josh — external-facing actions; T8 never acts on these unprompted)

Re-verified all four `docs/upstream/` drafts vs current upstream main **`a3ac713b7`** on
2026-07-12 (drift pass 2; see `findings-status.md` → "Re-verification 2026-07-12"). Upstream
only advanced 2 commits since the 2026-07-11 check, neither touching the finding files.
Only ONE remains fileable:

- [ ] **File upstream issue 1** (`docs/upstream/issue-1-flattened-init.md`) — `highlight.zig`
      `Flattened.init` compile bugs, re-confirmed STILL PRESENT on `a3ac713b7` (all three:
      `MultiArrayList(PageChunk)` :146, `.serial` :151, `.end_x` :158). The only draft worth
      filing. Requires: Josh vouched first (`contribution-process.md` — the vouch request must
      be human-written), then an Issue-Triage discussion, AI disclosure on it.
- [ ] **Board hygiene — `integrate` staging-workspace bookmark cleanup** (flag only; T8 never
      touches sibling workspaces). **CORRECTION to an earlier note:** `integrate` is **not** an
      orphan — it's an **active staging workspace** that lands cross-thread docs on bookmarks,
      then merges them via PR (new SHA), leaving the staging bookmark behind as redundant. Live
      bookmarks: `integrate/app-readme`, `integrate/jj-discipline` (its content is the current
      main tip `87b170b7`), `integrate/port-status-snapshot`; `integrate/resume-protocol` already
      resolved (deleted, content on main). Several read as "divergent" for this reason. Cleanup
      is **owner's job** (whoever runs `integrate`, or Josh): from *that* workspace, diff-verify
      each bookmark's content is fully on main, then `jj bookmark delete` + `jj git push
      --deleted`. Left untouched — active workspace + concurrent writer = exactly the divergent-
      change race the sibling-workspace guardrail prevents.
- Issue 2 — withdrawn (duplicate of upstream discussion #12769; do not file).
- Issue 3 — withdrawn (resolved upstream in `14c829883`; now a T5 port feature).
- Issue 4 — withdrawn (resolved upstream in `14c829883`; now a T1 port bug-mirror).
- [ ] **Enable branch protection** on `main` (suggested required checks: "Linux core (vt
      tests, release lane, clippy, fmt)", "macOS build + unit tests", "markdownlint
      (changed files)"). CI is green on main.

## Backlog / follow-ups

- [x] **Unpin the CI toolchain** (done, #49) — bumped `RUST_TOOLCHAIN` 1.96.0 → 1.97.0
      (explicit pin, not `stable`, so a rustc release can't red the shared gate mid-work).
      Verified `cargo clippy`/`fmt` green at 1.97 on both lanes. Also re-added
      `qwertty-term-font` to the Linux clippy allowlist (T2's fix landed; verified clean).
- [ ] **Consider a lightweight Linux-clippy local gate** for threads editing cross-platform
      crates. The `NavigateSearch` break (T3, 2026-07-12) and the earlier app-crate theme
      dead-code both slipped local gates because Linux clippy is CI-only (local gate is
      macOS-native). Options: document `cargo clippy --target x86_64-unknown-linux-gnu -p
      <crate>` in the shared gate for cross-platform-crate edits, or rely on branch protection
      + up-to-date-branch requirement (better — catches it centrally). Pairs with the
      branch-protection item above.

## Log

- 2026-07-11: session start; workspace `work/t8` created. CI bootstrap: verified locally
  that spike + renderer don't compile for Linux; that Rust 1.97 (2026-07-07) breaks the
  clippy gate with new lints (filed to T1/T2/T3 Inboxes); pinned CI to 1.96.0. Wrote
  `.github/workflows/ci.yml` + `docs/ci.md`. PR: t8/ci-bootstrap (#2).
- 2026-07-11: PR #2 CI caught two REAL issues no local gate had run: (1) Linux clippy
  `-D dead_code` on the app crate's macOS-gated theme code → scoped the Linux clippy lane
  to an explicit platform-independent crate allowlist (app clippy runs on the macOS job).
  (2) the termio `clean_exit_captures_code_and_runtime` test failed on BOTH runners
  (`runtime looks wrong: 2` Linux / `21` macOS for a 100ms sleep — a runtime *below* the
  sleep on every runner = real measurement bug) → filed to T4 Inbox, `--skip`ped on both
  test steps with a documented removal condition.
- 2026-07-11: **drift pass 1 COMPLETE** — `docs/upstream/drift.md` classifies the 92 unique
  upstream commits since our pin (31 mirror / ~39 feature / ~31 irrelevant) + the T1 perf
  mirror-target list. KEY perf finding: the biggest VT wins are ALREADY inside our pin
  `2da015cd6`, so T1's first move is verification, not mirroring. Re-verified the four
  upstream drafts vs current main: **issues 3 & 4 already FIXED upstream in `14c829883`**
  (OSC color-query lib-vt support + the color_operation reset leak); issue 1 still present
  (fileable), issue 2 still a dup. Updated `findings-status.md`; filed drift mirror-item
  pointers to T1/T2/T4/T5 Inboxes.
- 2026-07-12: PR #2 merged (Josh). While shipping the stacked drift PR #8, the
  `--delete-branch` on the #2 merge removed #8's base branch → GitHub auto-closed #8 (head
  branch + commits safe). The board had also moved a lot (T1 #9/#11/#12/#13/#15, T2 #7/#16,
  T4 #10/#14, T5 #4, T7 #18): my README jj-fix was made redundant by T1's #11 (dropped), and
  every status-file Inbox pointer had to be re-applied onto the live versions. Rebased drift
  onto current main, re-opening as a fresh PR → `main`.
- 2026-07-12: drift PR #44 merged (Josh go-ahead). Then shipped the CI toolchain unpin:
  verified `cargo clippy --workspace` (macOS lane) + the Linux allowlist (cross-target) +
  `cargo fmt` all green at `stable` = 1.97.0, so bumped `RUST_TOOLCHAIN` 1.96.0 → 1.97.0
  (kept an explicit pin rather than bare `stable` — a rustc release shouldn't red the shared
  gate mid-work). Also folded in the `qwertty-term-font` Linux clippy re-add (T2's non-macOS
  test fix landed; verified clean at 1.97). PR #49.
- 2026-07-12: PR #49's first CI run confirmed the toolchain bump is sound (Linux core incl.
  clippy@1.97 green; macOS clippy@1.97 green) but surfaced a SECOND flaky termio test —
  `throughput_cat_10mib` (asserts >40 MiB/s; dips under runner load, passes 3/3 locally).
  A shared gate that randomly reddens defeats the purpose, so `--skip`ped it on both test
  steps (consistent with the earlier `clean_exit` skip) and filed the CI-robustness fix to
  T4. Bundled into PR #49.
- 2026-07-12 session close: PR #49 merged (toolchain now 1.97.0 on main, font back in the
  Linux clippy lane, `throughput_cat_10mib` skipped + filed to T4). No open T8 PRs, no
  lingering bookmarks, workspace clean on main. Steady-state resting point: CI green, drift.md
  fresh (< 1 week), all four findings dispositioned. Relaunch T8 for the next drift pass or
  when pinged; the two Josh-gated items (file issue 1, enable branch protection) persist above.
- 2026-07-12 (cont): status-close PR #58's merge-with-main check caught main going
  Linux-clippy-red mid-session (T3's `NavigateSearch` unused-on-Linux import) — surfaced it,
  confirmed T3 had already fixed it on main (jjgf + trunk() check per Josh). Actioned T1's
  Inbox request: drift.md now marks `e44f5cb0f`/`8307349ec` DONE (#59, both confirmed latent
  in the port) and `b953bb346` no-work (already correct in port). Rolled these into the
  status-close PR; added a Linux-clippy-local-gate follow-up.
- 2026-07-12 (hygiene pass): checked `jj log -r 'divergent()'` — divergence exists but is
  entirely sibling threads' (T2 `qxolrnxu`/`slzvxsuo`/`lxopoprq`, T3 `vrnxqsrw`, T5 `ltqlnols`,
  T7 `xmqlquqm`/`nrvunzot`/`krxpqylp`), NONE mine. My change `ywoxrkss` (`t8/status-close`) is
  a clean single commit, not divergent. Left all sibling divergence untouched per guardrails;
  nothing of mine to abandon.
- 2026-07-12 (resume session): PR #49 confirmed merged (prior current-item done). CI: last
  completed main run green; badge (`ci.yml` on default branch) honest by construction. **Drift
  pass 2** (`a887df42c..a3ac713b7`): only 2 upstream commits, 1 touching a ported subsystem
  (`9659167ec` search-viewport fingerprint reuse = perf-only, irrelevant; `a3ac713b7` VOUCHED
  list = irrelevant). CLEAN, no must-mirror items, no Inbox lines filed; drift.md header +
  pass-2 note updated. **Findings re-verify** vs `a3ac713b7`: all four dispositions unchanged;
  Finding 1 spot-checked still live. **Board hygiene:** found orphan `integrate` workspace with
  a stale/redundant divergent `integrate/resume-protocol` (content already on main, no PR) →
  flagged to Josh in Pending approval (not touched — sibling workspace). **Shipped** the font
  `freetype` CI-coverage lane (T7 Inbox item) — macOS job now clippies + tests
  `-p qwertty-term-font --features freetype`; docs/ci.md "Optional-feature coverage" added.
  PR `t8/ci-freetype-coverage`.
- 2026-07-13 (closeout): auto-ship granted (Josh) → self-merged #108, #166, #221. Verified
  **0.2.0 released healthy** (release-plz + Trusted Publishing, crates.io sparse index confirms).
  Corrected the `integrate` finding (active staging workspace, not orphan). **Closeout jj
  hygiene:** resolved my divergent `vsoxzylm` (deleted the dangling origin branch
  `t8/ci-freetype-coverage` that #108's `--delete-branch` missed, abandoned the off-trunk twin
  `e9092f9d`; on-trunk `40208c8e` kept). Other divergent change-ids are T4/T5/T6/T7/`integrate`
  — not mine, left alone. Thread **CLOSED** (see Handoff); park dormant, wake on trigger.

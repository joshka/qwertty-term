# T2 status — Renderer completeness

(Stub created by T8 on 2026-07-11 to deliver Inbox items — T2: replace the header from
`TEMPLATE.md` on your first session and keep the Inbox.)

- **Inbox:**
  - 2026-07-11 (T8): `qwertty-term-font` fails clippy for non-macOS targets:
    `src/backend.rs:36` — `use super::*;` in the test module is unused because the only
    test is `#[cfg(target_os = "macos")]`. Move the import inside the test or cfg-gate it.
    Blocks re-including the font crate in CI's Linux clippy lane (`docs/ci.md`).
  - 2026-07-11 (T8): FYI — `qwertty-term-renderer` does not compile for Linux targets
    (e.g. `src/shaders/smoke.rs` imports objc2 unconditionally), so CI excludes it from
    the Linux lane. Fine if intentional (T7/Linux is parked); noting for awareness.
  - 2026-07-11 (T8): **drift pass 1** (`docs/upstream/drift.md`) — two T2-owned mirror
    fixes: `dac341cad` (font: cursor sprites used `cell_height` not `cursor_height` →
    `adjust-cursor-height` had no effect) and `d34b54e9b` (renderer-state mutex is unfair;
    termio relock loop starves the renderer thread — cross-territory with T4). Details in
    drift.md.

## Log

- (none yet)

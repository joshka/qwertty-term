# T2 status — Renderer completeness

- **Current item:** R6 slice 2 (scroll/pin tracking + viewport clip) — coded &
  gated locally, shipping via PR. Next: slice 3 (delete/eviction). Follow-ups in **#19**.
- **Last merged:** #7 (kitty R6 slice 1) + #16 (font non-macOS clippy) + #20 (status
  board), all on main as of 2026-07-12.
- **Blockers:** none. Workspace gate green.
- **Claims:** `crates/qwertty-term-vt/src/kitty/render.rs` (slice-2 rewrite of the
  resolver — screen-absolute positioning + signed rows + cull; additive, in my own
  earlier-claimed file). Drop on merge.
- **Inbox:** (other threads append requests here; owner triages into backlog)
  - 2026-07-11 (T8): `qwertty-term-font` fails clippy for non-macOS targets:
    `src/backend.rs:36` — `use super::*;` in the test module is unused because the
    only test is `#[cfg(target_os = "macos")]`. → **FIXED in PR `t2/font-linux-clippy`
    (moved the import inside the macOS-gated test).**
  - 2026-07-11 (T8): FYI — `qwertty-term-renderer` does not compile for Linux
    targets (e.g. `src/shaders/smoke.rs` imports objc2 unconditionally), so CI
    excludes it from the Linux lane. Fine if intentional (T7/Linux is parked);
    noting for awareness. → **Acknowledged; renderer is macOS-only this wave.**
  - 2026-07-11 (T6): `crates/qwertty-term-renderer/tests/shaping_throughput.rs`
    `dirty_vs_full_steady_state` is flaky under machine load (asserts
    `dirty_each <= full_each + 50µs`; fails intermittently in full-workspace runs,
    passes in isolation). → **Noted — it's a perf-timing test; flagging to T1/T6
    (perf owns the bound). Out of R6 scope; not touching it here.**
  - 2026-07-12 (T8): **drift pass 1** (`docs/upstream/drift.md`) — two T2-owned mirror
    fixes: `dac341cad` (font: cursor sprites used `cell_height` not `cursor_height` →
    `adjust-cursor-height` had no effect) and `d34b54e9b` (renderer-state mutex is unfair;
    termio's relock loop starves the renderer thread — cross-territory with T4). Details in
    drift.md.
  - 2026-07-12 (T7): **COORDINATION — ADR 003 P1 software backend (#41) needs a call on
    `gpu.rs`/`engine.rs`/`present.rs`.** Design + sizing:
    `docs/analysis/renderer-p1-software-backend.md`. TL;DR: adding a CPU `Software` backend
    requires *extending* `GpuBackend` — the trait covers resource creation only
    (`gpu.rs:21-27`), while `Engine` reaches frame/pipeline/pass/draw through concrete
    `Metal` inherent methods (`engine.rs:1198,1200,1481-1555`). The `Software` backend itself
    is a new, non-colliding module; the contentious part is the trait extension + Engine
    genericization in YOUR core, which R6 slice 2's viewport-clip work also touches.
    **Proposed (please accept/counter, §6 of the doc):** T7 authors the whole change; T2 owns
    the trait *shape* (review before build); sequence after your slice 2 merges (or shared
    rebase point) to avoid an `engine.rs` collision. No T7 edits to these files until you
    weigh in. Not urgent — slice 2 first is fine.

  - 2026-07-12 (T6): **#57 — cursor blink seam one-liner** in `engine.rs:347`:
    `from_snapshot_cursor(&c, c.blinking)` instead of the hardcoded `false`, once T5 adds
    `SnapshotCursor.blinking` (mode 12). Makes `FrameOptions.cursor_blink_visible` actually
    gate the cursor. It's in the `updateFrame` hot path slice 2 also touches — sequence
    after slice 2 / coordinate the rebase. Not urgent; details in #57.

## Log

- 2026-07-11: session start; workspace t2 created off main; beginning R6 slice 1
  scoping (engine kitty storage → renderer draw path).
- 2026-07-11: mapped renderer/engine/upstream via 3 explorers; design locked. The
  snapshot boundary (`SnapshotWindow`) carries NO kitty data → the live-app path
  (`from_window`) needs a later vt claim; slice 1 targets the
  `FullSnapshot::capture(&Terminal)` (test) path.
- 2026-07-11: R6 slice 1 DONE (PR #7). vt `kitty::render` resolver (pin + virtual
  placements, RGBA swizzle) + renderer `image` pipeline (upstream MSL verbatim) +
  generation-keyed texture cache + per-placement instance draws in both draw
  bodies. `FullSnapshot` carries resolved kitty data via the capture path. Tests:
  `kitty_image_pixels` offscreen readback, `dirty_equality` image scenario, 4 vt
  resolver unit tests (GPU-less). Design: `docs/analysis/renderer-r6-images.md`.
- 2026-07-11: rebased slice-1 line onto new main (T8 CI + T1 perf). Starting a
  clippy-cleanup PR to fix the pre-existing 1.97 lint drift across the workspace
  (per Josh) so the shared `-D warnings` gate is green.
- 2026-07-12: rebased PR #7 onto main (over T6 MB5 API polish — `render()`/`Frame`
  added to `engine.rs`; resolved as additive union). Ran two adversarial reviews
  (correctness/safety + GPU/shader/invariants): both SHIP, no defects. Applied their
  hardening: a `debug_assert` binding the engine-level image buffers to Sync mode
  (async would be a GPU use-after-free until they go per-slot), and extended the
  image dirty-equality test to add→re-generation→delete transitions. Full gate green
  on the rebased tree. Shipping.
- 2026-07-12: R6 slice 2 done. Rewrote `kitty::render::resolve_placements` to
  position images in absolute `Tag::Screen` rows against the window `scrollback_offset`
  rows up from the bottom (mirrors `snapshot_window`'s `window_top`): images track
  scrollback, partially-above ones get a negative signed `grid_row` (GPU rasterizer
  clips the top), fully-off ones are culled. Both pin-anchored and virtual (U=1)
  paths. Threaded `scrollback_offset` through `resolve_kitty`/`capture`. Tests:
  GPU-less `scrolled_placement_clips_top_and_culls` (cull / -1 clip / row-0 across
  offsets) + offscreen `kitty_image_scrolled_clips_top`. Full gate green. Shipping PR.
- 2026-07-12: workspace hygiene pass — rebased slice-2 line onto `trunk()`, then
  hand-abandoned two stale off-trunk divergent duplicates of my already-merged work
  (`4914ffd1` = slice-1 #7's dup; `b84f8d46` = status #20's dup), keeping the merged
  on-trunk side each. `divergent()` now clear of all T2 change-ids; remaining
  divergents (T3/T5 docs + a Resume-protocol doc) are not mine — left untouched.
- 2026-07-12: PR #64 (slice 2) BLOCKED on shared Linux CI: `qwertty-term-input`
  `binding::defaults::tests::len_is_reasonable` fails on Linux (`default_set().len()
  > 80` but the non-macOS binding set is ≤80). Pre-existing T3 platform bug, already
  filed in t3.md; not mine, not merging on red. macOS + markdownlint lanes pass.
- 2026-07-12: replied to T7 re #41 (GpuBackend ownership) in t7's inbox. Decision:
  T2 owns the `GpuBackend` trait shape AND authors the `Engine` genericization
  (engine.rs/gpu.rs/present.rs stay single-writer = T2, since R6 slices 3–5 rework
  the draw path); T7 authors the `Software` (tiny-skia) backend + Linux test un-gating
  (#42) against the trait. Sequencing: land the trait+genericization after slice 4
  (which reworks draw order); trait-shape sketch to T7 beforehand. No T7 edits to the
  renderer core until then.

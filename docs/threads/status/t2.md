# T2 status — Renderer completeness

- **Current item:** — (T2 shelf drained of unblocked standalone work). R6 kitty
  rendering COMPLETE. Waiting: T6 #57 cursor-blink (blocked on T5 `SnapshotCursor.blinking`);
  T7 GpuBackend trait-shape review (T7's PR-1 pending). Follow-ups in **#19**.
- **Last merged:** #50 doc-links + #141 closeout (#156); cursor-height regression tests
  (#158); mutex-starvation analysis (#163); R6 slices 1–5 (kitty COMPLETE); #141
  present-metric seam. All on main as of 2026-07-13.
- **Blockers:** none. Workspace gate green.
- **Claims:** none (slice 3 is renderer-only: `snapshot.rs` + `engine.rs`, my
  territory). GpuBackend trait/genericization owed to T7 after slice 4 (see t7 inbox).
- **Inbox:** (other threads append requests here; owner triages into backlog)
  - 2026-07-11 (T8): `qwertty-term-font` fails clippy for non-macOS targets:
    `src/backend.rs:36` — `use super::*;` in the test module is unused because the
    only test is `#[cfg(target_os = "macos")]`. → **FIXED in PR `t2/font-linux-clippy`
    (moved the import inside the macOS-gated test).**
  - 2026-07-11 (T8): FYI — `qwertty-term-renderer` does not compile for Linux
    targets (e.g. `src/shaders/smoke.rs` imports objc2 unconditionally), so CI
    excludes it from the Linux lane. Fine if intentional (T7/Linux is parked);
    noting for awareness. → **Acknowledged; renderer is macOS-only this wave.**
  - 2026-07-11 (T6): `crates/qwertty-term-renderer/tests/shaping_throughput.rs`
    `dirty_vs_full_steady_state` is flaky under machine load (asserts
    `dirty_each <= full_each + 50µs`; fails intermittently in full-workspace runs,
    passes in isolation). → **Noted — it's a perf-timing test; flagging to T1/T6
    (perf owns the bound). Out of R6 scope; not touching it here.**
  - 2026-07-12 (T8): **drift pass 1** (`docs/upstream/drift.md`) — two T2-owned mirror
    fixes: `dac341cad` (font: cursor sprites used `cell_height` not `cursor_height` →
    `adjust-cursor-height` had no effect) and `d34b54e9b` (renderer-state mutex is unfair;
    termio's relock loop starves the renderer thread — cross-territory with T4). Details in
    drift.md.
    → **BOTH RESOLVED (2026-07-13): neither needs a code change; both verified + documented.**
    `dac341cad` — already mirrored (the port applies both halves; ported from post-`dac341cad`
    upstream). Added `cursor_height_tests` regression coverage in `qwertty-term-sprite` (#158).
    `d34b54e9b` — does not structurally apply: the port renders off an owned `FullSnapshot` and
    holds the engine lock only for the microsecond snapshot copy, not across `update_frame`;
    residual risk empirically absent (DOOM-fire smooth). Analysis + T4 note in #163
    (`docs/analysis/renderer-mutex-starvation.md`). drift.md ticked for both.
  - 2026-07-12 (T7): **COORDINATION — ADR 003 P1 software backend (#41) needs a call on
    `gpu.rs`/`engine.rs`/`present.rs`.** Design + sizing:
    `docs/analysis/renderer-p1-software-backend.md`. TL;DR: adding a CPU `Software` backend
    requires *extending* `GpuBackend` — the trait covers resource creation only
    (`gpu.rs:21-27`), while `Engine` reaches frame/pipeline/pass/draw through concrete
    `Metal` inherent methods (`engine.rs:1198,1200,1481-1555`). The `Software` backend itself
    is a new, non-colliding module; the contentious part is the trait extension + Engine
    genericization in YOUR core, which R6 slice 2's viewport-clip work also touches.
    **Proposed (please accept/counter, §6 of the doc):** T7 authors the whole change; T2 owns
    the trait *shape* (review before build); sequence after your slice 2 merges (or shared
    rebase point) to avoid an `engine.rs` collision. No T7 edits to these files until you
    weigh in. Not urgent — slice 2 first is fine.

  - 2026-07-12 (T6): **#57 — cursor blink seam one-liner** in `engine.rs:347`:
    `from_snapshot_cursor(&c, c.blinking)` instead of the hardcoded `false`, once T5 adds
    `SnapshotCursor.blinking` (mode 12). Makes `FrameOptions.cursor_blink_visible` actually
    gate the cursor. It's in the `updateFrame` hot path slice 2 also touches — sequence
    after slice 2 / coordinate the rebase. Not urgent; details in #57.
  - 2026-07-12 (T6): **#50 remainder — broken intra-doc links in your renderer core.**
    `cargo doc -p qwertty-term-renderer -D warnings` fails on links in files you own
    (T6 fixed the `shaders/mod.rs` one in #123; these are all yours). Cheap to fold into
    an R6/trait PR since you're already in these files:
    - `metal/mod.rs:11` (the "Chunk R2 adds the frame lifecycle" module doc): bare
      `[`Frame`]`/`[`RenderPass`]`/`[`Pipeline`]`/`[`IOSurfaceLayer`]`/`[`Contents`]`/
      `[`Uniforms`]`/`[`SwapChain`]`/`[`RenderSnapshot`]`/`[`Grid`]` don't resolve in the
      module's doc scope — qualify each (`crate::metal::Frame`, `crate::cells::Contents`,
      `crate::wire::Uniforms`, `crate::swap_chain::SwapChain`, `crate::snapshot::RenderSnapshot`,
      `qwertty_term_font::Grid`, …). `[`Engine::draw_and_present`]` looks **stale** — verify the
      real method name (`present`? `draw_frame`?).
    - `snapshot.rs:331`: `[`SnapshotWindow`]` → `qwertty_term_vt::snapshot::SnapshotWindow`.
    Then a CI `cargo doc --no-deps -D warnings` lane (T8) would keep it green. Full list in #50.
    → **FIXED** (2026-07-13): qualified all 13 links across `metal/mod.rs`, `engine.rs`,
    `present.rs`, `snapshot.rs`. `Engine::draw_and_present` was **not** stale — the method is
    real (`present.rs:45`); it just needed `crate::engine::` qualification. `cargo doc
    -p qwertty-term-renderer --no-deps -D warnings` now clean. Shipping. (T8: the doc-lane
    is now worth adding — the crate is green.)
  - 2026-07-13 (T1): **DOOM-fire smoothness — renderer present-backpressure piece.**
    Present *pacing* landed in #139 (vsync `CADisplayLink` in the app); presents are now
    locked to vsync (~116 fps on 120 Hz), yet DOOM-fire still visibly judders. Diagnosis +
    handoff: [`docs/analysis/doomfire-smoothness.md`](../../analysis/doomfire-smoothness.md).
    The renderer half: our `IOSurface`→`CALayer` swap (`IOSurfaceLayer`) has **no vsync
    backpressure**, so render free-runs and the surviving frames are unevenly spaced in
    animation time. Ghostty presents via Metal `hasVsync()` with **drawable backpressure**
    (render blocks on the next `CAMetalDrawable`), so render is paced by the display and
    samples evenly. Fix direction: adopt a Metal drawable-present-with-vsync path, or
    throttle IOSurface swaps to the display-link cadence and render only the frame that
    will be shown. The io/coalescing half is T4's (see T4 inbox); the two compound. Not
    urgent, not T1's territory to build — flagging where it belongs. Related: T8 drift
    `d34b54e9b` (renderer-mutex starvation, already in your inbox above).
    → **T2 half: not pursuing the drawable-backpressure rewrite for now** (2026-07-13).
    Investigated (`docs/analysis/renderer-present-backpressure.md`, on main): post-#139 the
    present path is already vsync-driven; the remaining T2-side gap is fire-and-forget
    `setContents` with 1 frame in flight, and the drawable-present fix would **reverse locked
    decision 2** (IOSurface-on-CALayer, not `CAMetalLayer`) — too big to do speculatively.
    Instead I landed a **measurement seam** (`present_stats.rs`, env-gated
    `QWERTTY_TERM_PRESENT_STATS`) so judder is quantifiable (cadence mean±sd, CV) before any
    rewrite. Empirically DOOM-fire on main now renders **visibly smooth (~950–1120 fps, no
    judder)** in Josh's manual check, so the present-backpressure rewrite is **not justified
    on current evidence**. If judder resurfaces, run the metric (commands in the analysis doc)
    and the io/coalescing half (T4) is the likelier dominant term. Closing the T2 side of #141;
    metric stays on main as the tripwire.

## Log

- 2026-07-11: session start; workspace t2 created off main; beginning R6 slice 1
  scoping (engine kitty storage → renderer draw path).
- 2026-07-11: mapped renderer/engine/upstream via 3 explorers; design locked. The
  snapshot boundary (`SnapshotWindow`) carries NO kitty data → the live-app path
  (`from_window`) needs a later vt claim; slice 1 targets the
  `FullSnapshot::capture(&Terminal)` (test) path.
- 2026-07-11: R6 slice 1 DONE (PR #7). vt `kitty::render` resolver (pin + virtual
  placements, RGBA swizzle) + renderer `image` pipeline (upstream MSL verbatim) +
  generation-keyed texture cache + per-placement instance draws in both draw
  bodies. `FullSnapshot` carries resolved kitty data via the capture path. Tests:
  `kitty_image_pixels` offscreen readback, `dirty_equality` image scenario, 4 vt
  resolver unit tests (GPU-less). Design: `docs/analysis/renderer-r6-images.md`.
- 2026-07-11: rebased slice-1 line onto new main (T8 CI + T1 perf). Starting a
  clippy-cleanup PR to fix the pre-existing 1.97 lint drift across the workspace
  (per Josh) so the shared `-D warnings` gate is green.
- 2026-07-12: rebased PR #7 onto main (over T6 MB5 API polish — `render()`/`Frame`
  added to `engine.rs`; resolved as additive union). Ran two adversarial reviews
  (correctness/safety + GPU/shader/invariants): both SHIP, no defects. Applied their
  hardening: a `debug_assert` binding the engine-level image buffers to Sync mode
  (async would be a GPU use-after-free until they go per-slot), and extended the
  image dirty-equality test to add→re-generation→delete transitions. Full gate green
  on the rebased tree. Shipping.
- 2026-07-12: R6 slice 2 done. Rewrote `kitty::render::resolve_placements` to
  position images in absolute `Tag::Screen` rows against the window `scrollback_offset`
  rows up from the bottom (mirrors `snapshot_window`'s `window_top`): images track
  scrollback, partially-above ones get a negative signed `grid_row` (GPU rasterizer
  clips the top), fully-off ones are culled. Both pin-anchored and virtual (U=1)
  paths. Threaded `scrollback_offset` through `resolve_kitty`/`capture`. Tests:
  GPU-less `scrolled_placement_clips_top_and_culls` (cull / -1 clip / row-0 across
  offsets) + offscreen `kitty_image_scrolled_clips_top`. Full gate green. Shipping PR.
- 2026-07-12: workspace hygiene pass — rebased slice-2 line onto `trunk()`, then
  hand-abandoned two stale off-trunk divergent duplicates of my already-merged work
  (`4914ffd1` = slice-1 #7's dup; `b84f8d46` = status #20's dup), keeping the merged
  on-trunk side each. `divergent()` now clear of all T2 change-ids; remaining
  divergents (T3/T5 docs + a Resume-protocol doc) are not mine — left untouched.
- 2026-07-12: PR #64 (slice 2) BLOCKED on shared Linux CI: `qwertty-term-input`
  `binding::defaults::tests::len_is_reasonable` fails on Linux (`default_set().len()
  > 80` but the non-macOS binding set is ≤80). Pre-existing T3 platform bug, already
  filed in t3.md; not mine, not merging on red. macOS + markdownlint lanes pass.
- 2026-07-12: replied to T7 re #41 (GpuBackend ownership) in t7's inbox. Decision:
  T2 owns the `GpuBackend` trait shape AND authors the `Engine` genericization
  (engine.rs/gpu.rs/present.rs stay single-writer = T2, since R6 slices 3–5 rework
  the draw path); T7 authors the `Software` (tiny-skia) backend + Linux test un-gating
  (#42) against the trait. Sequencing: land the trait+genericization after slice 4
  (which reworks draw order); trait-shape sketch to T7 beforehand. No T7 edits to the
  renderer core until then.
- 2026-07-12: R6 slice 3 done (delete/eviction + storage-limit). Snapshot now carries
  the full live-image-id set (`storage.images.keys()`, cheap u32s) via a new
  `RenderSnapshot::kitty_live_ids`; `Engine::prepare_image_frame` retains only cached
  textures whose id is still live, so an explicit delete OR `image-storage-limit`
  eviction (both drop the image from `storage.images`) frees the GPU texture. Renderer-
  only (snapshot.rs + engine.rs), no vt change. Added `Engine::image_cache_len` accessor.
  Tests: GPU-less `kitty_live_ids_track_storage_deletes` (transmit 2 → delete 1 → delete
  all) + offscreen `kitty_image_eviction_on_delete` (cache 2→1→0, survivor keeps drawing).
  Full gate green. Shipping PR.

- 2026-07-13: #50 (broken intra-doc links) fixed — qualified all 13 links across
  `metal/mod.rs`, `engine.rs`, `present.rs`, `snapshot.rs` (module-doc `//!` scopes don't
  resolve bare type names / cross-crate names). Docs-only change; `cargo doc
  -p qwertty-term-renderer --no-deps -D warnings` clean, fmt clean. Also closed the T2 side
  of #141 (DOOM-fire present-backpressure) in the inbox: metric seam landed, rewrite not
  justified — DOOM-fire is visibly smooth on main (Josh confirmed). Shipping PR.

## Claims (active)

- 2026-07-12 (R6 slice 5): `crates/qwertty-term-vt/src/snapshot.rs` — ADD 3 kitty
  fields to `SnapshotWindow` + call a new `kitty::render::resolve_window` from
  `Terminal::snapshot_window[_tracking]` so the live-app (`from_window`) path carries
  kitty image data. Additive, does not touch existing fields or the `SnapshotCursor`
  area T5 plans to extend for #57 (blinking) — non-overlapping. Also editing my own
  `kitty/render.rs`. Dropping on merge. (T5 owns vt features; no active T5 claim here.)
- 2026-07-12: R6 slice 5 done (LIVE-APP rendering — the big one). Threaded kitty image
  data through `SnapshotWindow`: new vt `kitty::render::resolve_window` (placements +
  decoded RGBA + live-id set) called from `Terminal::snapshot_window[_tracking]`, so the
  window carries images. Unified the renderer: `FullSnapshot::{capture,capture_tracking,
  from_window}` all now convert from the window's kitty fields — deleted the old
  `resolve_kitty` duplication. Net: the running terminal (`app.rs` from_window path) now
  draws kitty images; before, only the test/capture path did. NO app logic change (one
  test fixture `selection.rs::blank_window` gained the 3 new empty fields — mechanical,
  from adding pub fields to SnapshotWindow). Tests: `kitty_image_via_from_window_live_app_path`
  (offscreen) + all existing kitty tests pass through the unified path + 22 vt snapshot
  tests unaffected. Per-frame RGBA copy accepted; `Image.data`→`Arc<[u8]>` to make it
  copy-free is a noted follow-up (#19). Full gate green. Claimed vt `snapshot.rs` (additive,
  non-overlapping with T5's SnapshotCursor.blinking plan). Next: slice 4 (z-order buckets).
- 2026-07-13: shipped three PRs (Josh authorized self-merge for like-for-like port work):
  #156 (#50 broken intra-doc links — qualified all 13; + #141 present-backpressure T2-half
  closeout), #158 (cursor-height `dac341cad` regression tests — verified already-mirrored),
  #163 (mutex-starvation `d34b54e9b` analysis — snapshot boundary mitigates, no code change;
  T4 notified). Then #141 concluded: present-metric seam (`present_stats.rs`) landed as the
  tripwire, drawable-backpressure rewrite deemed unjustified (DOOM-fire visibly smooth on
  main, ~950–1120 fps). Both T8 drift items for T2 (`dac341cad`, `d34b54e9b`) closed as
  no-code-change; drift.md ticked. T2 shelf now drained of unblocked standalone work.

# T5 status — VT completeness

(Stub created by T8 on 2026-07-11 to deliver Inbox items — T5: replace the header from
`TEMPLATE.md` on your first session and keep the Inbox. T5 is wave 2.)

- **Inbox:**
  - 2026-07-11 (T8): **drift pass 1** (`docs/upstream/drift.md`) — T5-owned mirror fixes in
    `qwertty-term-vt`: the selection/reflow width-bug cluster (`b6f34be44`, `607160657`,
    `a9f5b7eba`, `a55850c98`, `fa8cae88b` — end-pin columns built from desired/global width
    instead of the owning page's width) plus `0c299000f` (double-free of aliased tracked
    pins in `Screen.select`), `b287f6d1a` (grapheme-break replay `assert` fires after
    toggling mode 2027 mid-stream — Zig-assert-always-evaluates hazard), and the search
    subsystem fixes (`5d8eb78b7`, `5bc6588e4`, `627518447`). Full list + upstream `file:line`
    in drift.md.
  - 2026-07-11 (T8): **feature backlog** from drift.md — OSC color-query reporting in lib-vt
    (upstream `14c829883`, **our issue 3**: OSC 4/10/11/12 + Kitty OSC 21 replies via the
    `write_pty` effect); protocol-neutral clipboard rewrite (`634ef7198`); PageList
    generation-renewal / staleness detection (~14 commits). Not urgent; radar items.

## Log

- (none yet)

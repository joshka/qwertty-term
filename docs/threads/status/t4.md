# T4 status

- **Current item:** Mouse behaviors (mouse-hide-while-typing / focus-follows-mouse /
  right-click context menu) — user-selected as next after bell. NOTE: T3 is now active in
  the app crate (keybind dispatch slice b1 merged) — coordinate on view.rs/app.rs.
- **Last merged:** #82 bell (2026-07-12); #55 quick terminal; #14 OSC tab titles; #10 selection
- **Blockers:** none
- **Claims:** none (the `stream.rs` bell hook landed with #82; claim dropped. T1 ACKed it as
  additive/no-conflict under the vt file-split #70.)
- **Inbox:** (other threads append requests here; owner triages into backlog)
  - 2026-07-11 (T8): flaky/possibly-buggy test surfaced by CI. `qwertty-term-termio`
    `tests/exec.rs:309` `clean_exit_captures_code_and_runtime` asserts
    `notifier.runtime_ms >= 50` after the child runs `sleep 0.1; exit 7` (100ms). On the
    shared GitHub `macos-14` runner it reported `runtime looks wrong: 21` and failed the
    whole macOS CI job; it passes 5/5 locally with ~100ms runtimes. A runtime *below* the
    100ms sleep (not above) points at the exit watcher's runtime accounting not bracketing
    the full child lifetime — likely a real measurement bug, not just runner slowness.
    Please either fix the runtime measurement or make the assertion robust (e.g. compare
    against measured wall-clock, or widen the tolerance with a comment). CI currently
    `--skip`s this one test on the macOS job (see `docs/ci.md` → Known CI-only skips and
    `.github/workflows/ci.yml`); remove that skip once fixed.
    **T4 triage:** `qwertty-term-termio` is not this thread's territory (I own
    `crates/qwertty-term`), but the app is the exit watcher's sole consumer. Queued behind
    the current app-polish backlog; will pick up as a field-bug item (root-cause the
    runtime accounting, not just widen the assertion) unless T1/T8 take it first.
  - 2026-07-12 (T8): **drift pass 1** (`docs/upstream/drift.md`) — two T4-owned mirror fixes
    in upstream `termio/Exec.zig`: `bb0ac4c72` (pipelined pty gather slept the full ~1.2ms
    poll timeout after the parse stage went idle → doubled frame latency for request/response
    apps; self-pipe wake) and `d34b54e9b` (renderer-mutex starvation; call sites in Exec.zig,
    shared with T2). Details in drift.md.
  - 2026-07-12 (T8): second flaky termio test on shared CI runners —
    `qwertty-term-termio::throughput_cat_10mib` (`tests/exec.rs:555`) asserts `> 40 MiB/s`
    pipe throughput; dips below 40 under GitHub-runner load (passes 3/3 locally). Now
    `--skip`ped on both CI test steps (`docs/ci.md` → Known CI-only skips). Unlike
    `clean_exit_captures_code_and_runtime` this looks like plain timing noise, not a logic
    bug — please make it CI-robust (env-gate the throughput floor, or `#[ignore]` under CI)
    and drop the skip. Same owner as the other termio test flake above.

## Notes for T3 (config, Wave 2)

(Now tracked as issues [#30] (selection config keys) and [#34] (set_tab_title action).)

- `selection-word-chars`: gestures use the ported upstream default boundary set
  (`qwertty_term_vt::screen::DEFAULT_WORD_BOUNDARIES`); the config key needs the
  string→codepoints parser (upstream `Config.zig:6108`). Wire it through
  `Surface::selection_press/drag`'s `boundary_codepoints`.
- `click-repeat-interval`: currently `NSEvent.doubleClickInterval` with a 500ms
  fallback (`crate::gesture::click_interval`); the config key should override.
- `mouse-shift-capture`: selection currently assumes the `false` default
  (shift always overrides reporting); config + mode-toggle gating is open.

## Log (workspace hygiene)

- 2026-07-12: hygiene pass — `jj rebase -d trunk()` re-pointed my line cleanly onto
  current trunk. `divergent()` is clear of my change-ids (QT feature + docs(t4) status
  both single-sided and on trunk); remaining divergent commits are all other threads'
  residue (t2/t3/t5/t7/font/renderer) — left untouched per the guardrails. No abandons.

## Log

- 2026-07-12: MERGED #82 (bell). CI green (Linux core + macOS + markdownlint). vt bell-hook
  claim DROPPED (T1 ACKed it as additive under the vt file-split #70). Hit the stale-working-
  copy bug post-merge (cross-workspace) — no un-snapshotted edits, `update-stale` + rebase
  onto trunk clean, `divergent()` clear of my change-ids. Next: Mouse behaviors (user pick).
- 2026-07-12: Notifications slice 1 (bell) built — minimal claimed vt hook (stream.rs
  TerminalHandler pending_bell + bell() + take_bell, additive; T1 FYI'd), bell.rs
  (BellFeatures parser, unit-tested), bell-features config key, pace-tick fires system
  beep / dock attention / 🔔 title indicator (cleared on refocus), QWERTTY_TERM_SMOKE_BELL
  green. Deferred: audio + border features; OSC 9 notifications (bundle ADR), command-
  finish, OSC 9;4 progress (slice 2+). Full gate green on rebase. PR next.
- 2026-07-11: session start; workspace work/t4 created at main; beginning selection gestures.
- 2026-07-11: selection gestures built — SelectionGesture.zig port (gesture.rs), engine
  screen-space accessors (+ scrolled-viewport coordinate fix), vt select_word_between
  (claimed, minimal), controller wiring incl. shift-extend + pace-tick autoscroll,
  QWERTTY_TERM_SMOKE_SELECTION lane green (6 scenarios). Also fixed 3 pre-existing
  clippy-1.97 lints (vt/input) blocking the shared gate. PR next.
- 2026-07-12: PR #10 opened; rebased onto new main (dropped my lint commit — main's
  "Toolchain 1.97 lint sweep" superseded it); full gate re-run green on the rebase
  (all 9 smoke lanes incl. the new selection lane). Leaving PR open one visibility
  cycle per merge policy. Next item: OSC-synced tab titles.
- 2026-07-12: OSC tab titles — per-tab window/tab-label sync verified live, added the
  500ms ghost-emoji fallback (SurfaceView_AppKit.swift:286) + set-title-on-change,
  QWERTTY_TERM_SMOKE_TITLE lane green (hermetic via QWERTTY_TERM_COMMAND; the user's
  real shell races OSC titles otherwise). window-subtitle is GTK-only upstream
  (skipped); title-report default already matches (vt never answers CSI 21t);
  set_tab_title action left for T3's Binding.zig system. Stacked PR next.
- 2026-07-12: ship-it — rebased the stack onto new main (T1 print_slice perf commit);
  only conflict was this status file vs T8's seeded Inbox stub (merged, item preserved).
- 2026-07-12: MERGED #10 + #14 (both rebase-merged, CI green each). Cleaned post-merge jj
  divergence. Filed follow-ups #29/#30/#34.
- 2026-07-12: Quick terminal built — quickterm.rs geometry (Position/Size port, 12 pure
  tests), 4 config keys, borderless key-window subclass + NSAnimationContext slide + auto-
  hide, sentinel-TabId integration (in `tabs` for routing/render, out of registry for
  tab-nav/count), View→Toggle Quick Terminal (Cmd-`), ADR 0002 (global hotkey deferred:
  needs packaging + Accessibility). QWERTTY_TERM_SMOKE_QUICKTERM green (toggle
  in/out/re-in, geometry, live shell). Deferred to T3/config: quick-terminal-screen,
  spaces behavior, dock-conflict hide, size config-error strictness. PR next.
- 2026-07-12: MERGED #10 (selection gestures) then #14 (OSC tab titles) — both rebase-
  merged to main, CI green (Linux core + macOS build + markdownlint) on each. Cleaned up
  the post-merge jj change-divergence (abandoned the stale pushed copies). Filed three
  follow-up issues: #29 (vt tracked-pin gesture anchor — the documented deviation),
  #30 (wire selection-word-chars / click-repeat-interval / mouse-shift-capture config),
  #34 (set_tab_title action, needs T3 Binding.zig). Two backlog items shipped; next up:
  quick terminal.
- 2026-07-12: MERGED #55 (quick terminal). Rebased onto current main (T1 perf + keybind
  slices + CI toolchain changes); full gate re-run green on the rebase incl. all 11
  windowed smoke lanes. macOS build + markdownlint CI green; Linux core CI red ONLY from
  the pre-existing T3 `NavigateSearch` unused import (a crate #55 doesn't touch — main is
  already red there), so merged on the macOS check + local gate. Cleaned post-merge jj
  state. Three T4 backlog items shipped (selection, titles, quick terminal); next up:
  Notifications & command-finish.

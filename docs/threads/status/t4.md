# T4 status — App polish

(Stub created by T8 on 2026-07-11 to deliver Inbox items — T4: replace the header from
`TEMPLATE.md` on your first session and keep the Inbox.)

- **Inbox:**
  - 2026-07-11 (T8): flaky/possibly-buggy test surfaced by CI. `qwertty-term-termio`
    `tests/exec.rs:309` `clean_exit_captures_code_and_runtime` asserts
    `notifier.runtime_ms >= 50` after the child runs `sleep 0.1; exit 7` (100ms). On the
    shared GitHub runners it reported `runtime looks wrong: 21` (macos-14) and
    `runtime looks wrong: 2` (ubuntu-latest); it passes 5/5 locally with ~100ms runtimes.
    A runtime *below* the 100ms sleep on **both** runners (2ms and 21ms, not >100ms) points
    at the exit watcher's runtime accounting not bracketing the full child lifetime — a real
    measurement bug, not just runner slowness.
    Please either fix the runtime measurement or make the assertion robust (e.g. compare
    against measured wall-clock, or widen the tolerance with a comment). CI currently
    `--skip`s this one test on the macOS job (see `docs/ci.md` → Known CI-only skips and
    `.github/workflows/ci.yml`); remove that skip once fixed.

  - 2026-07-11 (T8): **drift pass 1** (`docs/upstream/drift.md`) — two T4-owned mirror
    fixes in `termio/Exec.zig`: `bb0ac4c72` (pipelined pty gather slept the full ~1.2ms
    poll timeout after parse went idle → doubled frame latency for request/response apps;
    self-pipe wake) and `d34b54e9b` (renderer-mutex starvation; call sites in Exec.zig,
    shared with T2). Details in drift.md.

## Log

- (none yet)

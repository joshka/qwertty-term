# T4 status

- **Current item:** — (12 features shipped; Window state + Notifications umbrellas
  COMPLETE; Resize overlay shipped. Next backlog: DOOM-fire io-coalescing/draw-split
  (T1 handoff #141, app/termio, overlaps the two T8 termio drift items — the biggest
  remaining item), Mouse slice 2 (focus-follows-mouse / middle-click), or the T8
  termio-flake fixes). T3 concurrently in the app crate (keybind dispatch b4).
- **Last merged:** #140 resize overlay (rebased onto T1's #139 vsync present pacing);
  #138 confirm-close-surface; #137 OSC 9;4 progress bar; #130 notify-on-command-finish
  (OSC 133); #125 desktop notifications (OSC 9/777); #117 window-state slice 1; #102
  clipboard hardening; #92 mouse; #82 bell; #55 quick terminal; #14 titles; #10 selection
- **Blockers:** none
- **Claims:** none. Two additive `stream.rs` `TerminalHandler` hooks have landed under the
  vt file-split #70 (no claim needed, both no-op-default + drain, no existing-handler change):
  the bell hook (#82, T1 ACKed), the desktop-notification hook (#125,
  `show_desktop_notification` + `take_notification`), the OSC 133 command-boundary hook
  (#130, `CommandBoundary` queue + `take_command_boundaries`), and the OSC 9;4 progress
  hook (#137, `progress_report` + `take_progress_report`; T1 FYI'd in each PR).
- **Inbox:** (other threads append requests here; owner triages into backlog)
  - 2026-07-12 (T1): **file-claim on `app.rs`** (Josh-authorized perf item) — CVDisplayLink
    present pacing. I split the render tick: io-service stays on a steady ~60Hz `NSTimer`
    (`Controller::tick_service`, background-safe so a hidden terminal still drains reply
    bytes), render moves to a vsync `CADisplayLink` (`tick_render`) via `NSView.displayLink`.
    Touched region: `tick`→`tick_impl(render:bool)` + new `tick_service`/`tick_render`,
    `start_service_timer`/`start_display_link`, `did_finish_launching` pacing wiring,
    `DelegateIvars.display_link`, two selectors, `Cargo.toml` CADisplayLink feature. GUI
    smokes keep the combined `NSTimer` tick (unchanged). Distinct from T3's config-core work.
    **MERGED as #139; claim dropped.** Rebased onto your #138 (kept both `smoke_confirmclose`
    and `display_link` ivars). Known follow-up documented for T2/T4: present pacing alone does
    not fully smooth high-fps animation (DOOM-fire still judders) — the deeper gap is
    renderer/io sampling (see T4/T2 inbox: IOSurface-swap vs Metal-vsync-backpressure + paced
    io application). Ping me if you need those lines meanwhile.
  - 2026-07-11 (T8): flaky/possibly-buggy test surfaced by CI. `qwertty-term-termio`
    `tests/exec.rs:309` `clean_exit_captures_code_and_runtime` asserts
    `notifier.runtime_ms >= 50` after the child runs `sleep 0.1; exit 7` (100ms). On the
    shared GitHub `macos-14` runner it reported `runtime looks wrong: 21` and failed the
    whole macOS CI job; it passes 5/5 locally with ~100ms runtimes. A runtime *below* the
    100ms sleep (not above) points at the exit watcher's runtime accounting not bracketing
    the full child lifetime — likely a real measurement bug, not just runner slowness.
    Please either fix the runtime measurement or make the assertion robust (e.g. compare
    against measured wall-clock, or widen the tolerance with a comment). CI currently
    `--skip`s this one test on the macOS job (see `docs/ci.md` → Known CI-only skips and
    `.github/workflows/ci.yml`); remove that skip once fixed.
    **T4 triage:** `qwertty-term-termio` is not this thread's territory (I own
    `crates/qwertty-term`), but the app is the exit watcher's sole consumer. Queued behind
    the current app-polish backlog; will pick up as a field-bug item (root-cause the
    runtime accounting, not just widen the assertion) unless T1/T8 take it first.
  - 2026-07-12 (T8): **drift pass 1** (`docs/upstream/drift.md`) — two T4-owned mirror fixes
    in upstream `termio/Exec.zig`: `bb0ac4c72` (pipelined pty gather slept the full ~1.2ms
    poll timeout after the parse stage went idle → doubled frame latency for request/response
    apps; self-pipe wake) and `d34b54e9b` (renderer-mutex starvation; call sites in Exec.zig,
    shared with T2). Details in drift.md.
  - 2026-07-12 (T8): second flaky termio test on shared CI runners —
    `qwertty-term-termio::throughput_cat_10mib` (`tests/exec.rs:555`) asserts `> 40 MiB/s`
    pipe throughput; dips below 40 under GitHub-runner load (passes 3/3 locally). Now
    `--skip`ped on both CI test steps (`docs/ci.md` → Known CI-only skips). Unlike
    `clean_exit_captures_code_and_runtime` this looks like plain timing noise, not a logic
    bug — please make it CI-robust (env-gate the throughput floor, or `#[ignore]` under CI)
    and drop the skip. Same owner as the other termio test flake above.
  - 2026-07-13 (T1): **DOOM-fire smoothness — io-coalescing / draw-split piece.** Follow-on
    to my #139 (vsync present pacing; claim already dropped above). Present timing is fixed
    but DOOM-fire still judders because the *content* is sampled unevenly: our gather→parse
    io pipeline applies the pty flood in bursts, so consecutive presented frames are 1/3/1
    animation-steps apart. Ghostty splits render from draw with a fixed `DRAW_INTERVAL`
    (~8 ms) and coalesces io wakeups → even cadence regardless of pty burstiness. Two
    upstream commits already in your inbox (T8 drift) are on this exact path: **`bb0ac4c72`**
    (pipelined pty gather slept the full ~1.2 ms poll timeout after parse went idle →
    doubled frame latency; self-pipe wake) and **`d34b54e9b`** (renderer-mutex starvation,
    shared with T2). Full diagnosis + both halves:
    [`docs/analysis/doomfire-smoothness.md`](../../analysis/doomfire-smoothness.md). The
    renderer-backpressure half is T2's (their inbox). Not T1 territory to build — flagging
    where it belongs; not urgent.

## Notes for T3 (config, Wave 2)

(Now tracked as issues [#30] (selection config keys) and [#34] (set_tab_title action).)

- `selection-word-chars`: gestures use the ported upstream default boundary set
  (`qwertty_term_vt::screen::DEFAULT_WORD_BOUNDARIES`); the config key needs the
  string→codepoints parser (upstream `Config.zig:6108`). Wire it through
  `Surface::selection_press/drag`'s `boundary_codepoints`.
- `click-repeat-interval`: currently `NSEvent.doubleClickInterval` with a 500ms
  fallback (`crate::gesture::click_interval`); the config key should override.
- `mouse-shift-capture`: selection currently assumes the `false` default
  (shift always overrides reporting); config + mode-toggle gating is open.

## Log (workspace hygiene)

- 2026-07-12: hygiene pass — `jj rebase -d trunk()` re-pointed my line cleanly onto
  current trunk. `divergent()` is clear of my change-ids (QT feature + docs(t4) status
  both single-sided and on trunk); remaining divergent commits are all other threads'
  residue (t2/t3/t5/t7/font/renderer) — left untouched per the guardrails. No abandons.

## Log

- 2026-07-13: MERGED #140 (resize overlay) — `cols ⨯ rows` HUD (NSTextField over the content
  view, rounded dark badge) on live resize, positioned per `resize-overlay-position`, auto-hidden
  after `resize-overlay-duration` (default 750ms); `resize-overlay` always/never/after-first;
  pure `resize_overlay.rs` (mode/position/text/origin, unit-tested). App-crate-only (upstream draws
  it apprt-side, not the renderer). Rebased onto T1's #139 (vsync CADisplayLink present pacing split
  tick into tick_service/tick_render) — resolved 2 app.rs `DelegateIvars` conflicts (kept smoke_resize
  + display_link) + the Cargo.toml quartz-core feature list; tick-body changes auto-merged (resize/
  progress/notify collection landed in tick_impl outside the render gate). QWERTTY_TERM_SMOKE_RESIZE
  green; progress smoke re-verified under the split tick. All 3 CI green. Merged PR-first, then status.
- 2026-07-13: MERGED #138 (confirm-close-surface, window state slice 2) — completes Window
  state. Tri-state config (false/true/always, default true); `Engine::cursor_is_at_prompt`
  passthrough to the vt's existing `cursorIsAtPrompt` port (APP-CRATE-ONLY, no vt change —
  the signal already existed). "Running" = cursor not at an OSC 133 prompt; no shell
  integration → output state → errs toward confirming (matches upstream exactly: OSC 133 is
  the SOLE signal, not pty pgrp). NSAlert on Cmd-W / context-menu Close Pane / windowShouldClose;
  OR-reduced over panes for window close; dead surface never confirms; close_confirm_hook for
  smokes. QWERTTY_TERM_SMOKE_CONFIRMCLOSE green. All 3 CI green (app-crate PR; macOS + markdownlint
  the real gates). Merged PR-first, then this status.
- 2026-07-13: MERGED #137 (OSC 9;4 progress bar, slice 2c) — completes the Notifications
  umbrella. ConEmu progress report (additive vt `progress_report` + `take_progress_report`
  hook, latest-wins) → pace-tick drain gated by `progress-style` → per-surface state with
  15s auto-clear → pure `progress.rs` display mapping (category + fraction + indeterminate,
  unit-tested) → in-surface CALayer bottom-strip overlay (app crate, renderer stays read-only;
  added objc2-core-graphics for CGColor + CATransaction feature). QWERTTY_TERM_SMOKE_PROGRESS
  green. All 3 CI checks green incl. Linux core (vt). Merged PR-first, then this status.
  Deferred: abnormal-command-exit-runtime; the real UNUserNotificationCenter banner (ADR 0003).
- 2026-07-13: MERGED #130 (notify-on-command-finish, slice 2b) — OSC 133 `C`/`D` boundary
  tracking (additive vt `CommandBoundary` queue + `take_command_boundaries`, exit code from
  the already-parsed `D`) → per-surface command timing in `Surface::pump` → mode/threshold
  gate (pure `notify.rs`: never/unfocused/always + bell/notify action + `-after`, unit-tested;
  `unfocused` uses real key-window + app-active focus) → bell and/or notification via the 2a
  seam+throttle. Config keys default `never` (off unless configured). QWERTTY_TERM_SMOKE_NOTIFYCMD
  green (succeeded + failed). All 3 CI checks green incl. Linux core (vt). Merged PR-first, then
  this status. Deferred: 2c OSC 9;4 progress bar (renderer/view-side); abnormal-command-exit-runtime.
- 2026-07-13: MERGED #125 (desktop notifications, slice 2a) — OSC 9 / OSC 777 parsed →
  drained → gated (`desktop-notifications`, core-level) → throttled (pure `notify.rs`
  NotificationThrottle: 1/sec global + 5s identical-dedup, unit-tested) → delivered
  (unbundled-safe fallback: dock attention + log; real UNUserNotificationCenter needs a
  signed bundle → ADR 0003, swaps behind the same seam). Additive vt hook
  (`show_desktop_notification`/`take_notification`, mirrors bell). QWERTTY_TERM_SMOKE_NOTIFY
  green. All 3 CI checks green incl. Linux core (vt touched this time). Merged PR-first,
  then this status. Deferred: 2b notify-on-command-finish (needs OSC 133 command-boundary
  tracking in vt), 2c OSC 9;4 progress bar.
- 2026-07-13: MERGED #117 (window-state slice 1) — quit-after-last-window-closed
  (default false on macOS; gates tick() terminate + applicationShouldTerminate…LastWindowClosed)
  plus initial geometry (window-width/-height cells → first window only via a Cell latch;
  window-position-x/-y). App-crate-only. QWERTTY_TERM_SMOKE_WINDOWSTATE green (live grid
  == configured 100x30). All 3 CI checks green (Linux core + macOS + markdownlint). Merged
  PR-first, then this status on merged main. confirm-close-surface deferred to slice 2
  (needs a running-process signal — OSC 133 / pty fg-pgrp — not plumbed through termio).
- 2026-07-12: MERGED #102 (clipboard hardening) — paste-protection (paste.rs classifier +
  NSAlert confirm), clipboard-trim-trailing-spaces (Engine::selection_string_opt), selection-
  clear-on-typing. App-crate-only. QWERTTY_TERM_SMOKE_CLIPBOARD green; CI all-green post-merge
  (merged on green local gate, CI confirmed). Applied the process lesson: merged the PR via gh
  FIRST, then wrote this status on the merged main (not stacked on the feature).
- 2026-07-12: MERGED #92 (mouse behaviors slice 1) — right-click context menu + mouse-hide-
  while-typing. NOTE: #92 auto-merged via a fast-forward when I stacked the status commit on
  the unmerged feature (harmless — app crate not in Linux CI, local gate green — memory saved).
- 2026-07-12: Mouse behaviors slice 1 built — right-click context menu (context_menu.rs pure
  model + view.rs menuForEvent:/dispatch; Copy-on-selection/Paste/Split×4/Close per-pane;
  full right-click-action enum) + mouse-hide-while-typing (NSCursor.setHiddenUntilMouseMoves
  on keyDown). App-crate-only, no vt. QWERTTY_TERM_SMOKE_MOUSE green (menu items + Split
  Right/Close Pane split/collapse). Deferred: focus-follows-mouse (tracking areas),
  cursor-click-to-move (OSC133), middle-click-action. Full gate green. PR next.
- 2026-07-12: MERGED #82 (bell). CI green (Linux core + macOS + markdownlint). vt bell-hook
  claim DROPPED (T1 ACKed it as additive under the vt file-split #70). Hit the stale-working-
  copy bug post-merge (cross-workspace) — no un-snapshotted edits, `update-stale` + rebase
  onto trunk clean, `divergent()` clear of my change-ids. Next: Mouse behaviors (user pick).
- 2026-07-12: Notifications slice 1 (bell) built — minimal claimed vt hook (stream.rs
  TerminalHandler pending_bell + bell() + take_bell, additive; T1 FYI'd), bell.rs
  (BellFeatures parser, unit-tested), bell-features config key, pace-tick fires system
  beep / dock attention / 🔔 title indicator (cleared on refocus), QWERTTY_TERM_SMOKE_BELL
  green. Deferred: audio + border features; OSC 9 notifications (bundle ADR), command-
  finish, OSC 9;4 progress (slice 2+). Full gate green on rebase. PR next.
- 2026-07-11: session start; workspace work/t4 created at main; beginning selection gestures.
- 2026-07-11: selection gestures built — SelectionGesture.zig port (gesture.rs), engine
  screen-space accessors (+ scrolled-viewport coordinate fix), vt select_word_between
  (claimed, minimal), controller wiring incl. shift-extend + pace-tick autoscroll,
  QWERTTY_TERM_SMOKE_SELECTION lane green (6 scenarios). Also fixed 3 pre-existing
  clippy-1.97 lints (vt/input) blocking the shared gate. PR next.
- 2026-07-12: PR #10 opened; rebased onto new main (dropped my lint commit — main's
  "Toolchain 1.97 lint sweep" superseded it); full gate re-run green on the rebase
  (all 9 smoke lanes incl. the new selection lane). Leaving PR open one visibility
  cycle per merge policy. Next item: OSC-synced tab titles.
- 2026-07-12: OSC tab titles — per-tab window/tab-label sync verified live, added the
  500ms ghost-emoji fallback (SurfaceView_AppKit.swift:286) + set-title-on-change,
  QWERTTY_TERM_SMOKE_TITLE lane green (hermetic via QWERTTY_TERM_COMMAND; the user's
  real shell races OSC titles otherwise). window-subtitle is GTK-only upstream
  (skipped); title-report default already matches (vt never answers CSI 21t);
  set_tab_title action left for T3's Binding.zig system. Stacked PR next.
- 2026-07-12: ship-it — rebased the stack onto new main (T1 print_slice perf commit);
  only conflict was this status file vs T8's seeded Inbox stub (merged, item preserved).
- 2026-07-12: MERGED #10 + #14 (both rebase-merged, CI green each). Cleaned post-merge jj
  divergence. Filed follow-ups #29/#30/#34.
- 2026-07-12: Quick terminal built — quickterm.rs geometry (Position/Size port, 12 pure
  tests), 4 config keys, borderless key-window subclass + NSAnimationContext slide + auto-
  hide, sentinel-TabId integration (in `tabs` for routing/render, out of registry for
  tab-nav/count), View→Toggle Quick Terminal (Cmd-`), ADR 0002 (global hotkey deferred:
  needs packaging + Accessibility). QWERTTY_TERM_SMOKE_QUICKTERM green (toggle
  in/out/re-in, geometry, live shell). Deferred to T3/config: quick-terminal-screen,
  spaces behavior, dock-conflict hide, size config-error strictness. PR next.
- 2026-07-12: MERGED #10 (selection gestures) then #14 (OSC tab titles) — both rebase-
  merged to main, CI green (Linux core + macOS build + markdownlint) on each. Cleaned up
  the post-merge jj change-divergence (abandoned the stale pushed copies). Filed three
  follow-up issues: #29 (vt tracked-pin gesture anchor — the documented deviation),
  #30 (wire selection-word-chars / click-repeat-interval / mouse-shift-capture config),
  #34 (set_tab_title action, needs T3 Binding.zig). Two backlog items shipped; next up:
  quick terminal.
- 2026-07-12: MERGED #55 (quick terminal). Rebased onto current main (T1 perf + keybind
  slices + CI toolchain changes); full gate re-run green on the rebase incl. all 11
  windowed smoke lanes. macOS build + markdownlint CI green; Linux core CI red ONLY from
  the pre-existing T3 `NavigateSearch` unused import (a crate #55 doesn't touch — main is
  already red there), so merged on the macOS check + local gate. Cleaned post-merge jj
  state. Three T4 backlog items shipped (selection, titles, quick terminal); next up:
  Notifications & command-finish.

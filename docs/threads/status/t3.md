# T3 status

- **Current item:** App-crate keybind dispatch **slice b5 shipping** — `chain=`
  multi-action dispatch (unified `resolve_actions` across the single-key + sequence
  paths). Keybind port essentially complete. Remaining follow-ups: sequence idle-timeout,
  flush-on-abort, and a windowed sequence smoke. Other T3 backlog: config-core, import.
- **Last merged:** #103 — keybind slice b4 (leader-key sequences `ctrl+a>c`)
- **Blockers:** none. App-crate dispatch coordinates with T4 (concurrently in `app.rs`
  on mouse/notifications — distinct regions; second-merger rebases).
- **Claims:** none (b5 claim dropped on merge).
- **Inbox:** (other threads append requests here; owner triages into backlog)
  - 2026-07-12 (T1): main CI RED on Linux since #52 — `defaults.rs` `NavigateSearch`
    import unused on non-macOS (used only in Darwin block) → `clippy -D warnings`
    fails "Linux core". **RESOLVED**: fully-qualified the two `NavigateSearch` uses
    and dropped the shared import (matching how `AdjustSelection`/`InspectorMode` are
    handled). Verified with `cargo clippy -p qwertty-term-input --lib --target
    x86_64-unknown-linux-gnu -D warnings` → 0 errors. Shipped with the reverse-map PR.
    Thanks T1. Root-cause note: my local gate is macOS-only; **added a Linux-target
    clippy step to my per-slice gate** so cfg-gated-import regressions can't recur.
  - 2026-07-12 (T7): main "Linux core" CI RED again — a *test*: `len_is_reasonable`
    asserts `default_set().len() > 80`, but the non-Darwin keymap (Cmd binds are
    macOS-gated) is smaller (~79) → panics on Linux. **RESOLVED**: made
    `len_is_reasonable` a platform-agnostic *range* (`70..=110`) that both the macOS
    (93) and non-Darwin (~79) sets satisfy; `macos_len_is_exact` (93) still pins macOS.
    Shipped with the `parse_and_put` PR. Thanks T7. Root cause: I can't *run* Linux
    tests locally (no cross-linker), and my Linux-clippy step only type-checks. Gate
    hardened two ways: (1) **cross-platform assertions must be ranges/`cfg`-exact, never
    one-platform floors**; (2) added `cargo clippy --all-targets --target
    x86_64-unknown-linux-gnu` (type-checks Linux *test* code too, no linking).
  - 2026-07-12 (T4): **app-crate gate OPEN.** T4 drained its Wave-1 hold on
    `crates/qwertty-term` — selection (#10), OSC tab titles (#14), quick terminal (#55)
    all merged; app crate at a clean green checkpoint. App-side keybind dispatch can
    start. T4 is concurrently on Notifications/command-finish touching
    `app.rs`/`termio.rs`/`engine.rs`/`menu.rs`; **file-claim** dispatch files before
    editing (hot: `crates/qwertty-term/src/{view.rs,app.rs,menu.rs,keybind.rs}`). Seam:
    `view.rs::try_handle_text_keybind` → `Controller::try_text_keybind_to_surface` +
    `crate::keybind`; `menu.rs::MenuAction` is the in-app action funnel. Second-merger
    rebases. (Triaged into backlog: app dispatch is my next slice — see Log.)

## Log

- 2026-07-11: session start; workspace t3 created off main; gate closed (no t4.md);
  began Phase A analysis.
- 2026-07-11: extracted pinned upstream sources at 2da015cd6 (Binding.zig, key.zig,
  key_mods.zig, Config.zig, Surface.zig, App.zig, apprt/action.zig, config/\*,
  cli/args.zig) into scratchpad; fanned out 5 research agents (Binding semantics,
  default keymap + dispatch, Rust current-state, config-core, 204-key option
  inventory). Captured Josh's real Ghostty config (both macOS locations) as the
  import acceptance fixture.
- 2026-07-11: wrote docs/analysis/{keybinds,config-core,import-ghostty-config}.md —
  Binding.zig study (85-action table with port status, verbatim macOS default
  keymap, ordered port slices that collapse the 4 bespoke tables), config
  load/reload-safety design note, and the full 204-key import mapping table.
  markdownlint clean.
- 2026-07-11: rebased onto current main (advanced past ec4f0753; T5 landing Phase A
  docs) and merged PR #6. Next: hold for wave gate, then keybind slice (a) model +
  parse + defaults (input crate, gate-safe).
- 2026-07-12: filed followups as issues — #22 (`copy-on-select` bool->enum, blocks
  clean import of Josh's config), #23 (wire `key-remap`; RemapSet ported but unused),
  #24 (T3 tracking issue: full keybind/config/import slice checklist).
- 2026-07-12: merged #47 — Binding.zig port slice (a): `qwertty-term-input::binding`
  (Trigger/TriggerKey + 10-rule parse, 85-action enum + Action::parse + scope(),
  full-binding Parser with flags/=-splitter/chains/sequences, Flags, Key::from_name;
  29 tests ported 1:1). Gate-safe input-crate work. NOTE: hit the cross-workspace
  update-stale bug mid-slice (working copy reset to ancient ec4f0753, binding files
  deleted on disk); recovered from a scratchpad backup + surgical re-apply onto real
  main. Next: `Set` storage/lookup + default keymap (slice a pt 2), then dispatch
  integration (slice b, gated on T4).
- 2026-07-12: slice (a) pt 2 — `binding::set` (case-folded `mods.binding()` forward
  map, 5-probe `get_event`, `put` overwrite, zero-dep `folded_codepoint`) + `defaults`
  (`default_set()`: 93 upstream-verified macOS default binds, non-Darwin block gated;
  replicates the `ctrl+shift+w` double-put and dual physical+unicode goto-tab quirks).
  Reverse map + sequences/chains deferred (#24). 214 input-crate lib tests green,
  clippy/fmt clean. Merged #52.
- 2026-07-12: slice (a) pt 3 — reverse action→trigger map on `binding::set`
  (`reverse: Vec<(Action, Trigger)>` — a PartialEq map, since `Action` has f32
  payloads and isn't Hash/Eq; `get_trigger` for menu accelerators, performable
  excluded, overwrite/remove fixup). 218 lib tests (4 new). `parse_and_put`
  (sequences/chains/unbind) remains the last input-crate slice before app dispatch.
  **Also folded in an urgent Linux-CI hotfix** for my #52 regression (see Inbox):
  `defaults.rs` `NavigateSearch` unused-import on non-macOS. Now Linux-target clippy
  is part of my gate.
- 2026-07-12: slice (a) pt 4 — `Set::parse_and_put`: full config-string application.
  Refactored the forward map to a `Value` enum (`Leaf`/`LeafChained`/`Leader(Box<Set>)`),
  added `>`-sequence (leader) storage, `chain=` (leaf→chained with reverse-map fixup),
  `unbind` with empty-leader pruning, validate-before-mutate, and `get_leader`/
  `get_chained` accessors for the future dispatcher. `chain_parent` is a trigger *path*
  (not upstream's raw pointers, which don't port to safe Rust). 229 lib tests (11 new);
  macOS + Linux clippy + fmt clean. **Input-crate keybind port is now feature-complete**
  (parse + Set + reverse map + defaults + parse_and_put); only app-crate dispatch
  integration remains (gated on T4). #24. Also folded in T7's Linux-CI test hotfix
  (`len_is_reasonable` range) — see Inbox.
- 2026-07-12: workspace hygiene — `jj rebase -d trunk()` then abandoned my two
  already-merged divergent duplicates (`pwszpltv` parse_and_put, `vrnxqsrw` docs
  #22-24); merged sides remain on trunk, work intact. Other threads' divergent
  changes left untouched. (Re: the flagged NavigateSearch Linux-CI failure — already
  fixed in #61; verified Linux clippy = 0 on current main, nothing to redo.)
- 2026-07-12: **slice b1 — first app-crate keybind dispatch** (wave gate open). Backed
  the user `keybind` text seam with the ported `Set`: `crate::keybind` is now
  `build_set` (`Set::parse_and_put` per config entry, warn-and-skip) + `resolve_text_bytes`
  (physical- then codepoint-probe → `text:` → `unescape_text`), retiring the bespoke
  `KeybindTable`/`TextKeybind`/trigger parser. `Controller.keybinds` is now a
  `binding::Set`. Behavior-preserving (only user `text:` dispatched; other actions
  parse but no-op at this seam until b2). Touched `keybind.rs`/`app.rs`/`config.rs`
  only — `view.rs` seam signature preserved. **Verified end-to-end**: keybind smoke
  passes in a real window (shift+enter marker 2×, plain enter → CR); tabkeys + search
  smokes green (untouched chord paths); offscreen smoke green; 213 app-lib tests,
  macOS clippy/fmt clean. Next (b2): fold `tabkeys`/`splitkeys`/`searchkeys` into the
  same `Set` and wire more actions. #24.
- 2026-07-12: **slice b2 — collapsed the three chord tables into the `Set`.** One
  lookup at `performKeyEquivalent:` (`view.rs::try_handle_keybind_chord` →
  `Controller::handle_keybind_chord` → `keybind::resolve_action` → the `Set`) replaces
  the separate search/split/tab `resolve` tables; `perform_keybind_chord` maps
  `binding::Action` → the existing tab/split/search handlers (handlers unchanged;
  `end_search` self-gates on an open search so plain Escape still reaches the pty).
  Deleted `DEFAULT_*_BINDINGS`/`resolve`/`*KeyBinding` from all three files (kept the
  action enums + `TabMods` + `RESIZE_STEP_PT`). The `Set` is now `default_set()` + user
  config, so **all four bespoke tables are retired** — the DoD proof. Behavior note:
  macOS split/search/tab chords are now upstream's exact defaults (the former non-mac
  placeholder aliases `ctrl+shift+o`/`e`, `ctrl+super+[`/`]` are dropped; `cmd+[`/`]`
  now navigate splits per upstream — re-addable via `keybind` config). **Verified**:
  tabkeys + search smokes drive `performKeyEquivalent:` → the new `Set` path and pass;
  splits + keybind + offscreen smokes green; 197 app-lib tests, macOS clippy/fmt clean.
  Next (b3): wire remaining action categories (font-size/scroll/clipboard/window) +
  leader-key sequence dispatch. #24.
- 2026-07-12: **slice b3 — `esc:`/`csi:` byte actions** at the keyDown seam.
  `keybind::resolve_text_bytes` now also emits `esc:` (`ESC`+value) and `csi:`
  (`ESC [`+value) in addition to `text:`, so the default `alt+left`=esc:b /
  `alt+right`=esc:f word-motion bindings (and any user `esc:`/`csi:`) work.
  keybind.rs-only (T3 territory, no T4 overlap — no claim needed). Survey finding:
  the other "remaining" default actions split into (a) **NSMenu key-equivalents**
  (font-size cmd+/-/0, copy/paste, new-win/tab, quit) — my `performKeyEquivalent:`
  returns false and the menu handles them, so no wiring needed; and (b) actions with
  **no Controller handler** (scroll_to_*, clear_screen, select_all, jump_to_prompt) —
  those are behavior implementation, a separate effort, not keybind wiring. Verified:
  esc:/csi: byte unit tests + keybind smoke green; 204 app-lib tests, clippy/fmt clean.
  Next: leader-key sequence *dispatch* (runtime), or the scroll/select_all behaviors.
- 2026-07-12: **slice b4 — leader-key sequence dispatch** (`ctrl+a>c`). Added
  `keybind::sequence_step` (navigate the `Set`'s Leader/Leaf storage along a trigger
  path → Leaf / Descend(trigger) / NoMatch) + `action_bytes` helper; `Controller`
  gained a `key_sequence: Option<Vec<Trigger>>` state + `handle_key_sequence` (the
  state machine) + `dispatch_keybind_action` (byte or chord leaf). `performKeyEquivalent:`
  runs the sequence check first, falling through to the single-chord path when the key
  isn't sequence-related (a root leaf isn't consumed here — so single-key/menu bindings
  are untouched). Verified: `sequence_step` unit test (descend→complete→abort) + all
  no-regression smokes (tabkeys/search/keybind/offscreen) green; 205 app-lib tests,
  clippy/fmt clean, workspace check + vt release green. **Deferred (follow-ups):** idle
  timeout that auto-cancels a dangling leader, flush-on-abort (upstream replays buffered
  keys; we drop them), a dedicated windowed sequence smoke, and `chain=` action dispatch.
  keybind.rs/app.rs/view.rs (claim #98). #24.
- 2026-07-12: **slice b5 — `chain=` multi-action dispatch.** `SeqStep::Leaf` now carries
  `Vec<Action>` and `sequence_step`/new `resolve_actions` check `get_chained`, so a
  chained leaf runs all its actions in order; `handle_keybind_chord` routes through
  `dispatch_keybind_action` (single action → menu-fallthrough preserved; chained → run
  all + consume) and takes `surface` (for byte actions in a chain). Removed the now-dead
  `resolve_action`. Verified: `chain=` unit test (`x=text:A`+`chain=text:B` → [A,B]) +
  no-regression smokes (tabkeys/search/keybind) green; 213 app-lib tests, clippy/fmt
  clean, workspace check + vt release green. **Keybind port essentially complete**
  (input crate + app dispatch b1–b5). Remaining: sequence idle-timeout/flush + windowed
  sequence smoke. claim #105. #24.

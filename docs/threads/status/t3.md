# T3 status — Config & keybinds

(Stub created by T8 on 2026-07-11 to deliver Inbox items — T3: replace the header from
`TEMPLATE.md` on your first session and keep the Inbox.)

- **Inbox:**
  - 2026-07-11 (T8): Rust 1.97 clippy `-D warnings` is red in `qwertty-term-input`:
    `src/key_encode.rs:964` (rewrite block with `?` operator). One-liner. CI
    (`.github/workflows/ci.yml`) is pinned to 1.96.0 until fixed — see `docs/ci.md`.
    (T3 is wave 2 — if a wave-1 thread wants to claim this first, coordinate via Claims.)

## Log

- (none yet)

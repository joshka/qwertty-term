# T6 status

- **Current item:** intra-doc links (#50) — the in-territory code ship; docs.rs slice 1 and
  injectable-clock done/routed
- **Last merged:** PR #51 (docs.rs metadata + embeddability quickstart) → main
- **Blockers:** none · **Open followup issues:** #21 (release 0.1.1, PENDING-APPROVAL),
  #50 (broken intra-doc links), #57 (cursor blink seam — routed to T5+T2)
- **Claims:** none
- **Inbox:** (other threads append requests here; owner triages into backlog)
  - 2026-07-11 (T1): heads-up — main (PR #9) now has the rustc/clippy-1.97 lint fixes.
    RESOLVED in the PR #5 rebase (identical fixes auto-collapsed).

## Log

- 2026-07-11: session start; workspace t6 created off main (ec4f0753)
- 2026-07-11: MB5 verified vs published 0.1.0 — item 1 (font error Display/Error) already
  shipped in 0.1.0; items 2–6 unfixed, all additive. Implemented: vt `terminal()/
  terminal_mut()/into_terminal()`; renderer `Frame` + `Engine::render()` +
  `Engine::for_grid`/`with_backend_for_grid` + `FullSnapshot::capture_live`; frame-capture
  example moved onto the new surface; CHANGELOG.md started. Refreshed
  work/betamax-thread-prompt.md for rename + published crates. Full gate green.
- 2026-07-11: rebased onto current main (T5 docs + PR #9 lint fixes); the clippy-1.97
  fixes I had carried were already on main and auto-collapsed → PR #5 scoped to MB5 API
  only.
- 2026-07-12: **merged PR #5** (Josh: "ship it") → main `60d0ee97`. MB5 list cleared
  (item 1 was already in 0.1.0; items 2–6 shipped additively). Followups filed as issues:
  **#21** (release 0.1.1 — PENDING-APPROVAL, the publish checklist lives there); flaky
  `dirty_vs_full_steady_state` already tracked in **#19**. Next: docs.rs quality pass.
- 2026-07-12: docs.rs pass slice 1. Diagnosed via live docs.rs: renderer 0.1.0 was built
  for `x86_64-unknown-linux-gnu`, so the macOS-gated `engine`/`metal` modules (the whole
  `Engine`/`render()`/`Frame` API) were MISSING from the published docs; same for font's
  CoreText layer. Fix: `[package.metadata.docs.rs]` pinning both to `x86_64-apple-darwin`
  (+ aarch64) so docs.rs renders the full API. Added an embeddability quickstart `no_run`
  doctest to the `engine` module (feed→capture_live→render→Frame) + a crate-root pointer.
  Filed #50 for the pre-existing broken/private intra-doc links found along the way (own
  focused pass). Doc-only + Cargo metadata; fmt/check/clippy/doctest green.
- 2026-07-12: merged PR #51 (docs.rs slice 1). Picked up injectable-clock completion — it
  resolves to cross-territory feature work, NOT my thin-API mandate: the renderer's blink
  seam (`FrameOptions.cursor_blink_visible`) is inert because `SnapshotCursor` doesn't carry
  mode 12; needs a vt snapshot field (T5) + an `engine.rs:347` one-liner (T2, hot slice-2
  file). Determinism itself already proven by frame-capture. Filed **#57**, routed to T5 +
  T2 Inboxes. Next in-territory ship: #50 intra-doc links.

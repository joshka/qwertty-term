# T6 status

- **Current item:** (pre-seeded; T6 fills in)
- **Last merged:** —
- **Blockers:** none
- **Claims:** none
- **Inbox:** (other threads append requests here; owner triages into backlog)
  - 2026-07-11 (T1): heads-up — main now has the rustc-1.97 lint fixes (PR #9:
    `context_signal.rs`, `key_encode.rs`, `terminal/tests.rs`, `spike/renderer.rs`).
    Your PR #5 carries its own versions of (some of) those fixes and will need a
    rebase onto current main; prefer main's hunks where they collide so the diff
    stays scoped to the MB5 API work.

## Log

- (thread will append from first session)

# T6 status

- **Current item:** MB5 API polish — PR #5 rebased onto current main, merging; next:
  docs.rs quality pass
- **Last merged:** none yet (PR #5 in flight)
- **Blockers:** none
- **Claims:** (all for `t6/mb5-api-polish`, drop on merge)
  - `crates/qwertty-term-renderer/src/engine.rs`, `snapshot.rs` — additive API only
    (`Frame`, `render()`, `for_grid`/`with_backend_for_grid`, `capture_live`); no
    internals touched (T2 territory)
  - `crates/qwertty-term-vt/src/stream.rs` — additive `Stream<TerminalHandler>`
    accessors only
- **Inbox:** (other threads append requests here; owner triages into backlog)
  - 2026-07-11 (T1): heads-up — main (PR #9) now has the rustc/clippy-1.97 lint fixes
    (`context_signal.rs`, `key_encode.rs`, `terminal/tests.rs`, `spike/renderer.rs`).
    RESOLVED: rebased PR #5 onto main; my duplicate fixes were identical and
    auto-collapsed, so PR #5 is now scoped purely to the MB5 API work.

## Log

- 2026-07-11: session start; workspace t6 created off main (ec4f0753)
- 2026-07-11: MB5 verified vs published 0.1.0 — item 1 (font error Display/Error) already
  shipped in 0.1.0; items 2–6 unfixed, all additive. Implemented: vt `terminal()/
  terminal_mut()/into_terminal()`; renderer `Frame` + `Engine::render()` +
  `Engine::for_grid`/`with_backend_for_grid` + `FullSnapshot::capture_live`; frame-capture
  example moved onto the new surface; CHANGELOG.md started. Refreshed
  work/betamax-thread-prompt.md for rename + published crates. Full gate green.
- 2026-07-11: rebased onto current main (T5 docs + PR #9 lint fixes); the clippy-1.97
  fixes I had carried were already on main and auto-collapsed → PR #5 now scoped to MB5
  API only. Merging. PENDING-APPROVAL: publishing 0.1.1 to crates.io (version bump lands
  in the release PR after Josh's go).

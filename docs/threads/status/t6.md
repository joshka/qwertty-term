# T6 status

- **Current item:** release-plz setup for the 0.2.0 release path (Josh un-held it), using
  crates.io **Trusted Publishing** (OIDC, no token secret). PR #180 open.
- **Last merged:** #175 (CHANGELOG → 0.2.0 sweep); earlier #123, #85, #80, #78
- **Blockers:** none · **Open followup issues:** #21 (release 0.2.0 — after #180, blocked
  only on Josh adding a crates.io Trusted Publisher per crate + the GH-token/branch-protection
  call)
- **Claims:** (for the release-plz PR, drop on merge)
  - `.github/workflows/release-plz.yml` — new CI workflow (T8's CI territory; noted in their
    inbox as FYI/handover offer)
  - `release-plz.toml` — new release config (T6 release mechanics)
- **Inbox:** (other threads append requests here; owner triages into backlog)
  - 2026-07-11 (T1): heads-up — main (PR #9) now has the rustc/clippy-1.97 lint fixes.
    RESOLVED in the PR #5 rebase (identical fixes auto-collapsed).

## Log

- 2026-07-13: post-0.2.0 follow-through. Verified docs.rs paid off: renderer 0.2.0 now
  renders the macOS `engine`/`metal`/`Engine` API (was invisible on 0.1.0's Linux build).
  #50 fully done (T2 shipped the renderer half, #156). Changelog model decision (Josh:
  hand-curated, release-plz hands-off): set `changelog_update = false` + rewrote CHANGELOG
  to a clean keep-a-changelog structure (dropped release-plz's sparse per-crate `[0.2.0]`
  tangle; folded #158 into 0.2.0 Fixed; added `[Unreleased]` with the 2 changes since 0.2.0).
  Note: v0.3.0 release PR (#206) is open — release self-serve, Josh's to merge.
- 2026-07-13: **🎉 0.2.0 SHIPPED — all 8 crates live on crates.io** via release-plz +
  crates.io Trusted Publishing (OIDC, no token). Flow that worked: Josh enabled the
  Actions-create-PRs setting + registered the 8 Trusted Publishers; release-plz opened #200
  at v0.2.0 (semver-check caught the SnapshotCursor break → minor bump); Josh merged #200 →
  the release job published all crates via OIDC. #21 closed. Release is now self-serve
  (conventional commits → release-plz PR → merge to publish). Changelog markdownlint-disable
  landed on main for future releases (#200 merged just before it, so the released 0.2.0
  changelog lacks it — cosmetic, moot). Next: docs.rs should build 0.2.0 (darwin metadata);
  betamax MB1 can drop path-pins for `qwertty-term-vt = "0.2"`.
- 2026-07-13: **release-plz first-run diagnosis + fixes.** The two release-plz runs failed:
  (1) 403 "GitHub Actions is not permitted to create or approve pull requests" — repo setting
  (Settings→Actions→General), Josh's to flip (harness auto-mode blocked me from `gh api`-ing
  it even with his approval); (2) release-plz computed `0.1.1`, not 0.2.0 — because I'd
  copied ratatui's `semver_check = false`, disabling break detection (pre-1.0, feats are
  patch, so no break marker → 0.1.1). Fix (this PR): re-enable semver-check (default) +
  install `cargo-semver-checks` in the release-pr job (tui-widgets pattern). renderer now
  builds on Linux (T7 ADR 003), so it's checkable; if a macOS-surface crate's *0.1.0
  baseline* fails to build on the Linux runner, we add a per-package `semver_check=false`
  then (visible in the next run). Josh drives the 403 + crates.io Trusted Publisher registration.
- 2026-07-13: **release-plz setup** (Josh un-held it) → PR #180. Aligned to the ratatui org's
  proven pattern (Josh pointed me at ~/local/ratatui as the reference): crates.io **Trusted
  Publishing** via `rust-lang/crates-io-auth-action` + `id-token: write` + a `release` GitHub
  environment — **no `CARGO_REGISTRY_TOKEN` secret**; SHA-pinned actions; `persist-credentials:
  false`; `toolchain: stable`. `release-plz.toml`: single root CHANGELOG, `release_always=false`,
  `semver_check=false`. Deferred: adopting ratatui's `cliff.toml` changelog automation (would
  reformat the hand-curated CHANGELOG — separate follow-up). **crates.io Trusted Publisher
  registration is Josh's** (external publish-access change; I declined to run it even with the
  ~/.cargo token present — supply-chain access control). Nothing publishes without a human
  merging the release PR.
- 2026-07-13: **#57 collision + CHANGELOG 0.2.0.** Josh pivoted the release to 0.2.0 and had
  me implement #57's vt `SnapshotCursor.blinking`. I built it (field + 3 populate sites + app
  fixture fix + test, full gate green) — but on rebase discovered **T2 had concurrently done
  the same #57 and merged it to trunk** (Josh authorized T2 to take it since T5 idle),
  INCLUDING the renderer one-liner + e2e tests. Abandoned my duplicate. Salvaged the unique
  piece: restructured CHANGELOG to **0.2.0** (Breaking = SnapshotCursor field; per-crate Added
  swept from git log since 0.1.0 — vt features, kitty rendering, keybinds, app features, docs).
  Lesson: re-scan sibling `Current item` right before starting cross-territory work; T2's
  status flipped from "#50" to "#57" between my scan and my start.
- 2026-07-12: shipped #123 (shaders doc link). Routed the #50 renderer remainder
  (`metal/mod.rs` "Chunk R2" cluster + `snapshot.rs:331` — both T2's files) to T2's
  inbox with exact fixes. That closes out T6's side of #50; the rest is T2's to fold into
  their R6/trait PRs. T6 now has no collision-free in-territory work left (all remaining
  items blocked on Josh/T2/T5).
- 2026-07-12: `cargo publish --dry-run` sweep — 6/8 clean; renderer+app fail standalone only
  on post-0.1.0 internal APIs (kitty/binding/word-boundaries), i.e. 0.1.1 must be a lockstep
  bump (release-plz's job). Recorded on #21. Then shipped the collision-free slice of #50:
  fixed `shaders/mod.rs`'s `PipelineDescription::vertex_buffer_index` → `VERTEX_BUFFER_INDEX`
  (stale link → the real module const). Remaining #50 renderer links are all in T2's active
  core (`metal/mod.rs`, `snapshot.rs`) — left for after T2's R6/trait work.

- 2026-07-12: per-crate READMEs — added `README.md` to the 6 crates that lacked one
  (vt/font/renderer/termio/input/ffi; sprite already had one), so crates.io landing pages
  render a real page instead of falling back to lib docs. Each: role, key API/usage,
  platform note, link to `docs/embedding.md`. cargo auto-includes README.md (confirmed via
  `cargo package --list`); markdownlint clean.

- 2026-07-12: parallel batch. (A) #78: font docs.rs metadata now enables `freetype` so
  T7's FreeType path shows on docs.rs. (B) MB4/embeddability doc refresh — new
  `docs/embedding.md` (the consolidated bytes→pixels embedding guide), feature-coverage
  embeddability section updated (MB5 → [x] shipped in #5/#51; MB4 render path [x] via
  frame-capture; injectable-clock note → #57), README pointer. (C) qwertty-collab doc:
  the `work/qwertty/ghostty-rs-collab.md` scratch file was never repo-tracked and its
  scratch copy is gone — not PR-able; noted, not fabricated.

- 2026-07-12: workspace hygiene — rebased onto trunk and abandoned my two off-trunk
  merged duplicates (`5114ed1b` #60 font-links, `3763e13f` #51 docs.rs quickstart; both
  merged sides live on trunk). `divergent()` now clear of my change-ids; remaining
  divergent commits belong to other threads, left untouched.

- 2026-07-11: session start; workspace t6 created off main (ec4f0753)
- 2026-07-11: MB5 verified vs published 0.1.0 — item 1 (font error Display/Error) already
  shipped in 0.1.0; items 2–6 unfixed, all additive. Implemented: vt `terminal()/
  terminal_mut()/into_terminal()`; renderer `Frame` + `Engine::render()` +
  `Engine::for_grid`/`with_backend_for_grid` + `FullSnapshot::capture_live`; frame-capture
  example moved onto the new surface; CHANGELOG.md started. Refreshed
  work/betamax-thread-prompt.md for rename + published crates. Full gate green.
- 2026-07-11: rebased onto current main (T5 docs + PR #9 lint fixes); the clippy-1.97
  fixes I had carried were already on main and auto-collapsed → PR #5 scoped to MB5 API
  only.
- 2026-07-12: **merged PR #5** (Josh: "ship it") → main `60d0ee97`. MB5 list cleared
  (item 1 was already in 0.1.0; items 2–6 shipped additively). Followups filed as issues:
  **#21** (release 0.1.1 — PENDING-APPROVAL, the publish checklist lives there); flaky
  `dirty_vs_full_steady_state` already tracked in **#19**. Next: docs.rs quality pass.
- 2026-07-12: docs.rs pass slice 1. Diagnosed via live docs.rs: renderer 0.1.0 was built
  for `x86_64-unknown-linux-gnu`, so the macOS-gated `engine`/`metal` modules (the whole
  `Engine`/`render()`/`Frame` API) were MISSING from the published docs; same for font's
  CoreText layer. Fix: `[package.metadata.docs.rs]` pinning both to `x86_64-apple-darwin`
  (+ aarch64) so docs.rs renders the full API. Added an embeddability quickstart `no_run`
  doctest to the `engine` module (feed→capture_live→render→Frame) + a crate-root pointer.
  Filed #50 for the pre-existing broken/private intra-doc links found along the way (own
  focused pass). Doc-only + Cargo metadata; fmt/check/clippy/doctest green.
- 2026-07-12: merged PR #51 (docs.rs slice 1). Picked up injectable-clock completion — it
  resolves to cross-territory feature work, NOT my thin-API mandate: the renderer's blink
  seam (`FrameOptions.cursor_blink_visible`) is inert because `SnapshotCursor` doesn't carry
  mode 12; needs a vt snapshot field (T5) + an `engine.rs:347` one-liner (T2, hot slice-2
  file). Determinism itself already proven by frame-capture. Filed **#57**, routed to T5 +
  T2 Inboxes. Next in-territory ship: #50 intra-doc links.
- 2026-07-12: #50 font half — fixed the broken/private intra-doc links in
  `coretext.rs`/`discovery.rs`/`tables.rs` (Presentation/PresentationMode qualified to
  crate root; private `Score`/`from_ct_font_at_size`/`to_ct_descriptor` unlinked; redundant
  FaceMetrics target dropped). `cargo doc -p qwertty-term-font -D warnings` now clean;
  fmt + font doctest green. Renderer half left in #50 (several sites in engine/snapshot —
  T2's active slice-2 files; do after slice 2 to avoid collision).
